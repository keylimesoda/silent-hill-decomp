# Enemy Rework Audit (2026-06-24)

> **Status — Audit/status report.** This is a dated source snapshot, not a current gameplay guarantee. Current AI/combat status and known gaps are in the [feature catalog](../../features.md); see also the [documentation index](README.md).

Read-only audit of every combat enemy/boss vs the known-good reference pattern
(groaner / puppet-nurse / grey-child / floatstinger). 38 agents, adversarially verified.
Generated from the `enemy-rework-audit` workflow. **No code changed by this audit.**

Verdict legend: **C**=confirmed by 2nd agent, **R**=refuted, **U**=uncertain (needs in-game), **?**=unverified.

## Reference correctness pattern (the four pillars)
### groaner (charaId 4) - reference regular enemy
- **Numbers:** HEALTH: base Q12(210.0f); bonus = Rng_Rand16() % Q12(70.0f) (0..70); Normal diff: 1/8 chance health*=2; Hard diff: 1/4 chance health*=2. Recoil-vs-stun threshold = Q12(100.0f) remaining health. health==0 + not targeted -> health=NO_VALUE, collision.state=0 (despawn).
DAMAGE DEALING: melee via func_8008A0E4(1, WEAPON_ATTACK(EquippedWeaponId_HuntingRifle, AttackInputType_Hold), ...), gated by ANIM_TIME_REL_KEYFRAME_IDX_GET(anim.time, 222) < 2u; jump-attack lunge speed MIN(var_v0, Q12(4.5f)).
ANIM TABLE: GROANER_ANIM_INFOS[48] (status range 0..47); s_seeds[48] in groaner_anim_infos.c; GroanerAnimInfos_Init() @ main_pc.c:527-528. status 0xFF = NO_VALUE sentinel. kind: 0=BlendLinear 1=PlaybackOnce 2=PlaybackLoop 3=NULL.
DISPATCH: sharedData_800EEE14_2_s00[10] indexed by controlState, [0]=NULL (init state). sharedData_800EEE3C_2_s00[3]={{0x1000,0x999},{0x6000,0x3000},{0x3000,0x4000}}.
KEYFRAME DATA: ~20 s_Keyframe arrays, PSX VRAM 0x800EEE14..0x800F04C0, from disc_extract/VIN/MAP2_S00.BIN; s_Keyframe = 20 bytes (10x s16 q3_12) identical PSX/PC.
STRUCT OFFSETS (s_SubCharacter, sizeof=296=0x128): model @0x0, position @0x18 (VECTOR3 q19.12), rotation @0x24, fallSpeed @0x34, moveSpeed @0x38, headingAngle @0x3C, flags @0x3E, field_44 @0x44, health @0xB0 (q19_12), damage @0xB4 (s_CharaDamage: position VECTOR3 @+0x0, amount q19_12 @+0xC, total 0x10), collision @0xC8 (s_CharaCollision), properties union @0xE8. e_PropsGroaner: controlState @0xE8. s_CharaCylinder: radius @0x0 (map-geo radius), field_2 @0x2 (chara-to-chara hit radius).
COMBAT GUARD: bodyprog_combat_8008A058.c:1522-1559 one-hit-per-swing bitmask (sp14 & sp10); player target sp10==NO_VALUE(-1).
- **Pattern:** The Groaner is a fully-faithful enemy port. Its correctness rests on FOUR pillars that every enemy must replicate:

1. COMPLETE DATA, NO ZERO-STUBS. Three separate data sources are all real and non-zero:
   - Anim infos: pc_port/src/groaner_anim_infos.c defines `s_AnimInfo GROANER_ANIM_INFOS[48]` (sized exactly to the status enum count, NOT [256]) and a `GroanerAnimInfos_Init()` registered in main_pc.c:527-528. The init is a RUNTIME patch loop because MinGW won't accept cross-TU function symbols (Anim_BlendLinear/PlaybackOnce/PlaybackLoop) as static initializers â€” so the array is zero-init at link and filled at boot from a `s_seeds[48]` table carrying {kind,status,hasVariableDuration,linkStatus,durationConstantQ12,startKeyframeIdx,endKeyframeIdx}. kind 0/1/2 -> Blend/Once/Loop fn-ptr, kind 3 -> NULL playbackFunc.
   - AI dispatch + collision keyframes: src/maps/characters/groaner_rodata.inc holds the real dispatch table `sharedData_800EEE14_2_s00[10]` (indexed by controlState, entry [0]=NULL is the init state handled by Ai_Groaner_Init), the sound-query table `sharedData_800EEE3C_2_s00[3]`, and ~20 `s_Keyframe[]` collision arrays extracted from disc (MAP2_S00.BIN @ PSX VRAM 0x800EEE14..0x800F04C0). s_Keyframe is 20 bytes on PSX and PC so arrays drop in with no reformat.
   - CRITICAL DLL RULE: groaner_rodata.inc is `#include`d at the BOTTOM of groaner.c (inside #ifdef SH_PC_PORT), NOT compiled into the exe, because the dispatch table points at sharedFunc_800Exxxx functions that are DLL-internal; the exe import lib can't see them, so the table must live in the SAME translation unit. The anim-info table, by contrast, lives in the exe (shared via import lib) since its fn-ptrs are exe-resident.

2. LIVE REGISTRATION via per-map header charaUpdateFuncs. Groaner_Update is wired into each hosting map's `.charaUpdateFuncs[]` at the Chara_Groaner slot (e.g. map2_s00_header.c:164-165, map2_s02/map4_s02/map5_s01/map6_s00). groaner.c is compiled LIVE per map (it's #included via the rodata pattern in each map DLL). Signature: `void Groaner_Update(s_SubCharacter* groaner, s_AnmHeader* anmHdr, GsCOORDINATE2* coords)`.

3. ONCE-PER-SWING, FPS-SAFE DAMAGE DEALING. Groaner deals damage by calling the shared melee primitive `func_8008A0E4(1, WEAPON_ATTACK(EquippedWeaponId_HuntingRifle, AttackInputType_Hold), groaner, &pos, &player, rot, Q12_ANGLE(90.0f))` gated by `ANIM_TIME_REL_KEYFRAME_IDX_GET(anim.time, 222) < 2u` â€” a RANGE window relative to a keyframe, never `== exactKf`. func_8008A0E4/func_8008A058 itself carries the SH_PC_PORT one-hit-per-swing guard (bodyprog_combat_8008A058.c:1522-1559): a per-swing already-hit bitmask `(sp14 & sp10)` zeroes damageAmount+knockback on repeat frames (PSX applied unconditionally because its 2-frame hitbox window read as a single hit; on PC the window spans the whole swing). For the player target sp10==NO_VALUE(-1) so `(sp14 & sp10)==sp14` latches the hit. Chainsaw/RockDrill are explicitly exempt (continuous weapons). Damage is then applied via `target->damage.amount += damageAmount` exactly once.

4. REAL FLOOR/COLLISION FROM DATA, AIRBORNE TRACKING. Movement+floor handled in sharedFunc_800E5AA4_2_s00: applies g_GravitySpeed to fallSpeed (halved during JumpAttack), calls `func_8005CB20(groaner, &collResult, field_F0, field_F2)` to resolve motion+floor, then sets/clears GroanerFlag_Airborne by comparing `position.vy == collResult.surface.groundHeight`. Per-keyframe hit/collision shapes come from the extracted s_Keyframe data via `Collision_CharaCollisionSet(groaner, &kf0, &kf1)` and the CopyData macro (radius = real field data, NOT a hardcoded PC fallback), followed by `Chara_CollisionShapeOffsetsUpdate`.

DAMAGE-TAKING is data-driven and correct: health initialized in Ai_Groaner_Init = `Q12(210.0f) + (Rng_Rand16() % Q12(70.0f))` (base 210, up to +70 bonus), with difficulty multipliers (Normal 1-in-8 chance x2, Hard 1-in-4 chance x2). On hit, sharedFunc_800E33DC_2_s00 does `health = MAX(health - damage.amount, Q12(0.0f))`, picks recoil vs stun by remaining health thresholds (>100 = stand-recoil, <=100 = stun-to-death path), then `Chara_DamageClear`. Death: when health hits 0 and not airborne, transitions to a death anim and controlState GroanerControl_Death; when health==0 and no longer the targeted NPC, health=NO_VALUE and collision.state=0 (despawn). controlState==0 sentinel triggers one-time Ai_Groaner_Init.

ALL movement/rotation/timer arithmetic is g_DeltaTime / TIMESTEP_ANGLE / Q12_MULT_PRECISE based (frame-rate independent), and the whole update is gated `if (g_DeltaTime != Q12(0.0f))` so it freezes cleanly on pause. The only SH_PC_PORT additions inside groaner.c are: a cached getIndex() (MIPS eval-order safety), NULL-guards on the dispatch entry-0 call, and diagnostic logging â€” i.e. the game logic is otherwise untouched clean decomp.

### puppet_nurse (16) + puppet_doctor (18) - reference, but may take too little damage (die too fast)
- **Numbers:** HEALTH (s_pnInstSeeds in puppet_nurse_data.c, raw=Q12, /4096 for game units): doctor idx0 = 3072000 = 750HP (field_4 stagger-accum=300, field_2C knockback=20480/5.0); nurse idx1 = 1638400 = 400HP (field_4=350, field_2C=30720/7.5); nurse idx2 = 2662400 = 650HP (field_4=150); nurse idx3 = 3276800 = 800HP (field_4=350). Variant select in PuppetNurse_Init: doctor always charStatIdx=0 (750); nurse charStatIdx=((stateStep-1)%3)+1 -> 1/2/3 (400/650/800) by spawn palette.
PER-HIT DAMAGE: combat sets var_s0 = Q12(D_800AD4C8[weaponAttack].field_4) (item_screens_3.c:77; field_4 = 3rd table column int). Summed into target->damage.amount (bodyprog_combat_8008A058.c:1573), then PuppetNurse_DamageHandle: newHealth = health - damage.amount (clamped >=0). Handgun field_4=0x64=100 -> Q12(100)=409600 dmg/hit; other handgun-class row 0x015E=350; melee rows ~0x0082=130/0x00C8=200. Nurse v1 400HP/409600 = ~4 handgun hits; doctor 750 = ~7-8. PSX-faithful.
DEATH/STAGGER (PuppetNurse_DamageHandle, field_118 FSM): case0: newHealth<=Q12(120.0f)=491520 -> dying (field_118++, controlState 3 doctor /4 nurse death-react; if ==0 also Savegame_EnemyStateUpdate). Else stagger if controlState!=2 AND (field_124->field_4 < accumulated field_114 OR single hit damage.amount > Q12(320.0f)=1310720) -> controlState=2 flinch. case1: force health=0, death SFX, anim 14->22.
ANIM/COLLISION: 46-entry s_AnimInfo (A/B), field_18 scale 4096/4485, attack weaponAttack id field_20=57/58 via func_8008A0E4 90deg arc, cylinder.radius base Q12(0.3f) recomputed per frame from bone matrices. Struct growth PC: s_AnimInfo 16->32B, s_800D5710 0x34->0x38B.
- **Pattern:** The puppet_nurse pair is a GOOD, complete reference. (1) Data is fully extracted, not zero-stubbed: pc_port/src/puppet_nurse_data.c provides real `sharedData_800D5710_3_s03[4]` (per-variant instance table with health), two parallel 46-entry anim_info arrays (`s_animInfoA`/`s_animInfoB`), and `sharedData_800D5A8C_3_s03[3]` combat tuning â€” using the seed-table + runtime-init pattern (PnAnimSeed/PnInstSeed) because MinGW can't take exe-resident playback-fn pointers as static initializers. (2) `PuppetNurseData_Init()` IS registered in main_pc.c (lines 546-547). data_stubs.c:1256 only carries a COMMENT redirecting to the real file (no residual zero-stub). (3) The damage path is FPS-correct: per-hit damage is gated once-per-swing by the shared bitmask guard in bodyprog_combat_8008A058.c (sp14 & sp10, lines 1522-1559) â€” the canonical high-FPS fix. Nurse health subtraction happens once per accumulated `damage.amount` in PuppetNurse_DamageHandle, with Chara_DamageClear at the end of every update. This is the pattern other enemies should match.

### grey_child (8) - reference regular enemy (note: no rodata.inc, no anim_infos.c - where is its data?)
- **Numbers:** charaId: GreyChild=8, Mumbler=9, Stalker=7 (all -> Stalker_Update, map0_s00_header.c:148-149). Health (Normal diff): sharedData_800E3A20 = Q12(350) set in Stalker_Update; *1.5 only on MAP6_S03 non-easy. Hard: 600. Easy/Normal regen rate sharedData_800E3A28 = 0/3, Hard 6 per dt. Flee threshold sharedData_800E3A24 = Q12(100) (Hard 180). Rage/attack timer cap sharedData_800E3A2C = Q12(50) Normal / 35 Easy / 100 Hard. Init anim: status=ANIM_STATUS(StalkerAnim_30,true), time=Q12(477), keyframeIdx=477 (idle/walk loop, kf 477-506). Grab anims: StalkerAnim_34 (grab-from-behind) / StalkerAnim_35 (grab-from-front), each 48-frame blend, kf 461-476; chosen by ABS(player.rotation.vy - stalker.rotation.vy) > Q12_ANGLE(90). Lunge/scratch = Control_5, active kf window 50-64 (62-64 stop, 50-61 close distance), Chara_MoveSpeedUpdate Q12(6.0). Attack engage distances (Control_2): grab when distToPlayer 0.5-1.0 & angle<60deg; lunge when dist<0.9 & angle<90deg & dist>0.7-or-1in4. distToPlayer<0.7 -> Control_4. Collision keyframe rodata: s_Keyframe = 20 bytes; arrays sharedData_800DDBBC[9], 800DDC70[7], 800DDCFC[7], 800DDD88[16], single 800DDBA8. STALKER_ANIM_INFOS has 104 rows for 48 logical anim states. Overlay base 0x800C9578; STALKER_ANIM_INFOS PSX addr ~0x800DD5A8.</concrete_numbers>
<parameter name="files_read">["C:/Claude/silenthill/silent-hill-decomp/include/bodyprog/chara/chara.h","C:/Claude/silenthill/silent-hill-decomp/src/maps/map0_s00/map0_s00_2.c","C:/Claude/silenthill/silent-hill-decomp/src/maps/map0_s00/map0_s00_header.c","C:/Claude/silenthill/silent-hill-decomp/src/maps/map0_s00/map0_s00_anim_info.c","C:/Claude/silenthill/silent-hill-decomp/src/maps/map0_s00/Chara_Stalker.c","C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/stalker.c","C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/stalker_rodata.inc"]
- **Pattern:** Grey Child legitimately has NO grey_child.c, NO grey_child_anim_infos.c, and NO grey_child_rodata.inc because it is not a distinct enemy implementation â€” it is one of three skins of the Stalker family that all share the SAME update function and data. This is the pattern for "an enemy that needs no separate data file": its code/data are reused from a sibling enemy.

1. SHARED UPDATE FUNC. In map0_s00_header.c the charaUpdateFuncs table maps slots 7 (Stalker), 8 (GreyChild), 9 (Mumbler) ALL to Stalker_Update (header.c:148-149). All AI/anim/movement/damage logic lives in src/maps/characters/stalker.c (Stalker_Update @0x800D2F08), compiled LIVE into map0_s00 via wrapper src/maps/map0_s00/Chara_Stalker.c which does #include "../src/maps/characters/stalker.c". stalker.c is therefore LIVE (has a compiling wrapper) â€” edit stalker.c, not a per-enemy file.

2. REGION SELECTS THE SKIN, NOT THE CODE. map0_s00_2.c:16-21 picks charaId by region via #define ENEMY_CHARA_ID: NTSC -> Chara_GreyChild(8), NTSCJ -> Chara_Mumbler(9). Grey Child and Mumbler are the SAME enemy in a different region build (different model/texture/anim asset files via CHARA_FILE_INFOS, identical behavior). The full Stalker(7) uses the same state machine but a different per-map branch: Ai_Stalker_Control_2 is gated #if defined(MAP0_S00) (the alley grey-child variant), Ai_Stalker_Control_3 is #if !defined(MAP0_S00) (the full hospital/town stalker). Grey Child = the MAP0_S00-compiled, simpler branch.

3. ANIM DATA lives in the DECOMP MAP SOURCE, already non-zero â€” NOT in pc_port. STALKER_ANIM_INFOS[] (104 rows) is defined in src/maps/map0_s00/map0_s00_anim_info.c:7 and exported to all map DLLs via --export-all-symbols. Registered in Ai_Stalker_Init via ModelAnim_AnimInfoSet(&stalker->model.anim, STALKER_ANIM_INFOS) (stalker.c:183). No *AnimInfos_Init() in main_pc.c needed because the table is real decomp data compiled into the map, not a pc_port stub.

4. COLLISION RODATA was a real pc_port fix. The per-frame body/hit-box keyframes (sharedData_800DDBA8/DDBBC/DDC70/DDCFC/DDD88_0_s00, s_Keyframe = 20 bytes each) live in src/maps/characters/stalker_rodata.inc, #include'd at the bottom of stalker.c under #ifdef SH_PC_PORT. PSX resolved these from the loaded overlay; PC had only zero stubs in data_stubs.c, which collapsed the melee VERTICAL hit window so short grey children could never be struck (and gave the family no push radius). They are fed to Collision_CharaCollisionSet in sharedFunc_800D70C4_0_s00 (the per-anim collision setter, stalker.c:2367). This is the canonical "zero-stub table -> un-hittable enemy" fix, just delivered as an .inc on the shared file rather than a per-enemy file.

5. FPS-SAFE patterns are correct throughout. Movement uses g_DeltaTime scaling (Q12_MULT_PRECISE(g_DeltaTime, ...)), turn rates use TIMESTEP_ANGLE, and all combat/anim windows use ANIM_TIME_RANGE_CHECK(time, lo, hi) RANGES â€” never == exactKeyframe and never raw frame counts. Damage TAKEN is applied once-per-event in sharedFunc_800D3308_0_s00 (health -= damage.amount, then Chara_DamageClear) â€” not per-frame. There is one PC div-by-zero guard already added (g_DeltaTime==0 -> speed sample 0, stalker.c:582-591). This is the reference shape every other enemy's combat gating should match.

### floatstinger (15) - reference BOSS
- **Numbers:** Health: Q12(4000) normal/easy, Q12(6000) Hard. Per-frame damage intake cap field_108: Q12(300) normal, Q12(500) easy. Phase/stagger thresholds: field_F8 > 1000 (-> stagger controlState 3), field_FA > 1200 (-> stagger + FloatstingerFlag_1 enrage). Anim table: FLOATSTINGER_ANIM_INFOS[28]; idle hover = FloatstingerAnim_9 (status ANIM_STATUS(9,true)), kf 191..222 loop; charge attack = FloatstingerAnim_1 kf 1..31; dive/spit = FloatstingerAnim_12 kf 1..31. Charge anim duration set per-difficulty: Q12(26) easy / Q12(32) normal / Q12(38) Hard. Attack windows: Anim_1 melee fires func_8008A0E4 during anim.time 17..25; Anim_12 projectile fires after time>=18. Knockback gate: shared sp14 & sp10 once-per-swing bitmask (bodyprog_combat_8008A058.c ~L1545-1558). Collision cylinder radius Q12(0.4) or Q12(0.5) recomputed each frame from GTE-projected anchors; box.bottom Q12(-1.46) (alive) / Q12(-2.0) (player dead). Control-state dispatch D_800D7A04[7]: {NULL, NULL, func_800D1BF8, func_800D2B90, func_800D341C, func_800D3424, func_800D3564}. Death: controlState FloatstingerControl_6, EventFlag_350. Damage-rate constant D_800D785C: Q12(2.4) Hard / Q12(3.2) normal / Q12(4.0) easy. attack weapon id = EquippedWeaponId_Unk59.
- **Pattern:** A boss is structurally a CHARACTER with a BIG health pool plus its own multi-handler control-state machine, but it reuses the SAME shared infrastructure as regular enemies: real extracted anim infos (FLOATSTINGER_ANIM_INFOS[28] + FloatstingerAnimInfos_Init registered in main_pc.c), an extracted AI dispatch table (D_800D7A04 in floatstinger_rodata.inc, included at bottom of floatstinger.c so the map DLL gets it), the shared damage/knockback emitter func_8008A0E4, and the shared one-hit-per-swing guard in bodyprog_combat_8008A058.c. It is LIVE via #include "../src/maps/characters/floatstinger.c" in map4_s05.c and registered as Floatstinger_Update in map4_s05_header.c charaUpdateFuncs[].

KEY BOSS-SPECIFIC TRAITS vs a regular enemy:
1. HEALTH: huge and difficulty-scaled. Floatstinger_Init sets health = Q12(4000) normal/easy, Q12(6000) on Hard (vs regular enemies ~tens-hundreds). Health is NEVER reduced directly by the shared combat code; instead func_800D1968 reads floatstinger->damage.amount each frame, caps how much damage a single accumulation can contribute (field_108 capped at Q12(300) normal / Q12(500) easy â€” a damage-rate limiter so the boss cannot be bursted down), subtracts into health, clears damage via Chara_DamageClear, and plays a hurt SFX. Player_DisableDamage is used so health clamps to 1 (not 0) while the player is mid-action, then to 0 to allow death.

2. DAMAGE-DRIVEN PHASE/STAGGER STATE MACHINE: accumulated damage thresholds (field_F8 > 1000, field_FA > 1200) flip controlState from FloatstingerControl_2 (normal hover/attack) to FloatstingerControl_3 (stagger/recoil), and field_FA>1200 additionally sets FloatstingerFlag_1 (enraged-stagger variant). This is the "multi-phase" mechanism â€” phases are gated on cumulative damage, not a single HP bar split. Death sets controlState to FloatstingerControl_6 and sets EventFlag_350.

3. CONTROL-STATE DISPATCH: Floatstinger_ControlUpdate calls `D_800D7A04[controlState](floatstinger)`. Handlers: [2]=func_800D1BF8 (main hover + dive/spit attacks), [3]=func_800D2B90 (stagger + procedural wing-stroke), [5]=func_800D3424 (appear/intro), [6]=func_800D3564 (death). Indices 0,1 are NULL. PC-port note: PSX passed the chara implicitly in a0; the PC table is typed (s_SubCharacter*) and the call site passes it explicitly.

4. ATTACK / KNOCKBACK: two attacks, both gated on anim.time windows (NOT exact keyframes). FloatstingerAnim_1 (charge/swoop) fires func_8008A0E4 once during time window 17..25 with WEAPON_ATTACK(EquippedWeaponId_Unk59, AttackInputType_Tap), passing GTE-transformed wing-tip world positions sp20[0]/[1] as the hit origin and two ratan2 angles for knockback direction. FloatstingerAnim_12 (dive/projectile spit) fires sharedFunc_800CB0A4_4_s03 to launch projectiles (more on Hard via FloatstingerFlag_0, spawning 2 extra spread shots). Knockback magnitude/direction flows through func_8008A0E4 -> var_s7 applied to target->damage.position, and is rate-limited to ONCE PER SWING by the shared sp14 & sp10 bitmask guard. The reason air_screamer 'shoves but barely damages' is the inverse of this working pattern: its attack must register damageAmount > 0 through func_8008A0E4 (real weapon-attack table entry) AND its knockback must be gated once-per-swing â€” if damage is zero/missing the player only gets the knockback (shove) with no health loss, and without the once-per-swing guard high FPS multiplies the shove every frame.

5. COLLISION/HIT SIZING is fully data/GTE-driven per frame: func_800D3AD4 recomputes the boss's box top/height/offsetY and cylinder radius (Q12(0.4)/Q12(0.5)) from three GTE-projected body anchor points (D_800DB928 hierarchy via Vw_CoordHierarchyMatrixCompute + gte_rt), and shrinks the cylinder during the charge anim's time window â€” the hit radius is never a hardcoded PC fallback. This is the contrast to broken enemies using a hardcoded radius.

6. FLOOR/FALL handling: func_800D35F0 moves via Collision_WallDetect + a custom floor-clamp func_800D4458 (3 floor boxes D_800D780C) and clamps vy to floor; func_800D4A3C drives approach/fall toward the player with clamped fallSpeed â€” the boss is a flyer so it manages its own vertical clamp rather than relying on generic chara gravity.

7. PROCEDURAL ANIMATION: wings are driven two ways â€” keyframe playback (anim.status loops) AND a procedural wing-stroke in func_800D2B90/func_800D37E8 writing D_800DB8A8[15] from phase D_800D7858, applied to bone matrices via Math_RotMatrixZxyNegGte/MulMatrix. PC-port fix: D_800D799C is an ALIAS into FLOATSTINGER_ANIM_INFOS[idle].duration.constant (the game writes wing-flap speed through this every frame); a zero-stub disconnect froze the idle wings.

8. DRAW/OT: per-frame draw markers (func_800D3F84) clamp into a fixed OT bucket (g_OrderingTable0[idx].org[12]) and only emit when sp48.vz >= GeomScreen distance â€” PC adds an extra (sp48.vz > 0) guard to keep x86 idiv from dividing by zero. No unbounded otz-derived OT index.

HIGH-FPS CORRECTNESS: every timing gate uses anim.time RANGE windows (e.g. time>17 && time<25, time>0 && time<18) and g_DeltaTime-scaled accumulators, NOT '== exactKeyframe' or raw frame counts. The damage/knockback emit is once-per-swing via the shared bitmask. This is why it works at uncapped FPS.

### shared damage/anim/collision/registration infrastructure
- **Numbers:** e_CharaId enum (chara.h:55-113): None=0,Harry=1,AirScreamer=2,NightFlutter=3,Groaner=4,Wormhead=5,LarvalStalker=6,Stalker=7,GreyChild=8,Mumbler=9,HangedScratcher=10,Creeper=11,Romper=12,Chicken=13(unused),SplitHead=14,Floatstinger=15,PuppetNurse=16,DummyNurse=17,PuppetDoctor=18,DummyDoctor=19,Twinfeeler=20,Bloodsucker=21,Incubus=22,Unknown23=23,MonsterCybil=24,LockerDeadBody=25,Cybil=26,EndingCybil=27,Cheryl=28,Cat=29,Dahlia=30,EndingDahlia=31,Lisa=32,BloodyLisa=33,Alessa=34,GhostChildAlessa=35,Incubator=36,BloodyIncubator=37,Kaufmann=38,EndingKaufmann=39,Flauros=40,LittleIncubus=41,GhostDoctor=42,Parasite=43,Padlock=44. Combat var_a3 blood-type table (bodyprog_combat_8008A058.c switch lines 1599-1681): Harry=1, AirScreamer/NightFlutter=2, Stalker=4, LarvalStalker/GreyChild/Mumbler=3, Creeper=5, Groaner/Wormhead=6, SplitHead=7, HangedScratcher=8, Floatstinger/Twinfeeler=10, Romper=9, PuppetNurse/PuppetDoctor=11, Bloodsucker=12, default=16, Padlock=NO_VALUE. Player damage: MAP0_S00 = damage.amount*2 (line 8651); Easy = *3>>2 (line 8658), Hard = *6>>2 (line 8662). Death timers (vc_main.c): DEATH_TIME_MAX clamp line 416-418, ENEMY_DEATH_TIME_MAX=Q12(1.5f) line 900. Collision default groundHeight when no IPD chunk = Q12(8.0f) (collision.c:489); cylinder.position.vy = chara->position.vy - Q12(0.02f) (line 484). Melee max engage distance Math_Distance2dGet > Q12(3.0f) skip (combat line 1917).
- **Pattern:** Damage is TWO-PHASE and decentralized. Phase 1 (accumulate): the SHARED melee/attack function func_8008BA9C (file bodyprog_combat_8008A058.c) does NOT decrement health. It only ADDS into target->damage.amount (line 1573: target->damage.amount += damageAmount;) plus damage.position knockback, sets target->attackReceived = weaponAttack, and spawns blood via func_8005F6B0. Phase 2 (apply+death): each enemy's OWN per-frame update function reads damage.amount, does health = MAX(health - damage.amount, 0), then calls Chara_DamageClear(chara) to zero damage.amount. The canonical reference is groaner.c sharedFunc_800E33DC_2_s00: line 162 early-out if damage.amount==0, line 183 `groaner->health = MAX(groaner->health - groaner->damage.amount, Q12(0.0f));`, line 206 Chara_DamageClear. The PLAYER's apply path is the mirror in player_control.c lines 8651/8666 (`player->health -= player->damage.amount` with MAP0_S00 x2 and difficulty scaling at 8655-8664), death at 8669-8673 (health<0 -> NO_VALUE + g_Player_IsDead). So 'too much/too little damage to kill' bugs (split_head, nurse/doctor) live in the PER-ENEMY apply function, NOT the shared combat func. The once-per-swing high-FPS gate is a PC-port block in bodyprog_combat_8008A058.c lines 1522-1559: sp10 = 1<<npcIndex (target bit, line 1318; NO_VALUE/-1 for player at 1300), sp14 = attacker->field_44.field_8 (the per-swing already-hit bitmask, line 1295), and if ((sp14 & sp10) && !_continuous) it zeroes damageAmount and var_s7 knockback. The latch is set at line 1587 (sp14 |= sp10) -> field_44.field_8. _continuous exempts only Chainsaw/RockDrill. The bitmask is RESET to 0 at swing start in func_8008A0E4 (lines 160/165/422/429 set field_44.field_8 = 0). High-FPS keyframe helpers are SH_AIM_KF_REACHED / SH_AIM_KF_REACHED_P (player_control.c 3297/3309: `>=` under SH_PC_PORT, `==` otherwise) and TIMESTEP_30_FPS (game.h:20) / TIMESTEP_SCALE_30_FPS (math.h:89) for per-frame scalar motion. Collision/floor: Collision_CharaCollisionSetup (collision.c:474, 0x80069FFC) builds the collision cylinder from chara->position + chara->collision.shapeOffsets/box, and if Ipd_CollisionDataGet returns NULL (no IPD chunk under the chara) calls Collision_DefaultResultSet with groundHeight=Q12(8.0f) and returns true (line 487-491) -- the fall-through-floor root when a chunk isn't loaded. Ground attach is via Collision_GroundProbeRadial (collision.c:405) + Collision_SurfaceGet (212). The melee LOS uses Ray_TraceQuery (ray.c:18, 0x8006D90C), NOT Ray_LosHitCheck/Ray_LineCheck (a merge mis-map fixed at combat 1957, player_control 8990/9001, air_screamer 824). charaUpdateFuncs registration: s_MapOverlayHdr.charaUpdateFuncs[Chara_Count] at map.h:522, populated per-map in <map>_header.c (e.g. map0_s00_header.c:147 indexes by e_CharaId, NULL for absent). Called by Game_NpcUpdate (npc_main.c:332) at the single dispatch site npc_main.c:702. A character .c is LIVE iff a per-map wrapper does `#include \"../src/maps/characters/<name>.c\"` (giving it a build object); twinfeeler.c is the lone DEAD file (live worm is inline in map4_s03.c func_800D3694).

## Per-enemy results
### air_screamer (id 2) — role: regular
- **Status:** BROKEN attack-dealing (matches user report). Enemy is LIVE, correctly registered in 6 maps, has real non-zero anim infos (inline AIR_SCREAMER_ANIM_INFOS[56]) and real rodata (sharedData_800CAA98 via INCLUDE_RODATA). Both prior fixes (a9aa3ea52 rodata, 311f49281 radius) are PRESENT. Damage-TAKING, floor/collision, anim, and registration dimensions are clean. The 'barely attacks, mostly shoves' symptom has a clear root cause: on PC the bone-vertex pointers ptr_D48[1..3] used to build each attack's cone hitbox are NULL, so the vanilla damage pipeline (func_8008A3E0) is skipped for ALL four attack anims (StandBite/HoverBite/GlideScratch/HoverInjuredBite). The PC fallback that replaces it only recognizes ONE anim (HoverBiteAttack) universally; the common GlideScratchAttack swoop and the other bites deal zero damage while the (oversized, hardcoded field_2=1.5) collision cylinder still pushes Harry. Fix = recognize all four attack anims in the fallback OR (preferred) populate cone params from AS position when ptr_D48 is NULL and restore the func_8008A3E0 path, plus shrink the hardcoded push radius field_2.
- Live: ['C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/air_screamer.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map2_s00/Chara_AirScreamer.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map0_s01/Chara_AirScreamer.c']
- **Missing fixes:** ["damage_dealing: the vanilla cone attack pipeline (func_8008A3E0) is bypassed on PC for ALL FOUR attack anims because ptr_D48[1..3] bone pointers are NULL and the cone params (field_44.field_18/field_C/field_E) are never populated from a PC-resident source -- the canonical 'route enemy attack through func_8008A0E4/func_8008A3E0 with the shared one-hit-per-swing guard' fix is only partially emulated (HoverBiteAttack + map0_s01 stateStep)", 'hit_radius/shove: chara-to-chara push radius cylinder.field_2 still hardcoded Q12(1.5) in the PC fallback (air_screamer.c:13737) -- the sibling of the radius fix that was applied; should be data-derived']
- **[C] critical/damage_dealing** (conf high) `C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/air_screamer.c:13593-13663 (vanilla cone path skipped) + :93-178 (top-level PC fallback only covers HoverBiteAttack)`
    - ROOT CAUSE of 'barely attacks, mostly shoves'. The Air Screamer has FOUR damage-dealing attack anims: StandBiteAttack(1), HoverBiteAttack(2), GlideScratchAttack(3), HoverInjuredBiteAttack(9). On PSX, sharedFunc_800D82B8/sharedFunc_800D7EBC sets the cone hitbox per-anim (func_800805BC writes the cone tip pos into field_44.field_18 and angles into field_C/field_E) then calls func_8008A3E0(airScreamer) to apply damage (lines 13624, 13659). On PC, EVERY one of those cases bails out early via `if (sharedData_800CAA98_0_s01.ptr_D48[N] == NULL) break;` (lines 13614, 13639-13640) because the reformatter NULLs the bone-vertex pointers ptr_D48[1..3]. So func_8008A3E0's cone test runs against stale/zero cone params (field_18/field_C never written) and never connects -- the ENTIRE vanilla attack pipeline deals zero damage on PC for all four attack anims. The developer bolted on a proximity fallback, but the universal top-level one (line 97/155) only fires when anim.status == HoverBiteAttack(2). GlideScratchAttack (the flying swoop-scratch, the cafe AS's most common attack, set at line 7829), StandBiteAttack, and HoverInjuredBiteAttack therefore deal NO damage on ANY map -- yet the AS still flies into Harry and his collision cylinder pushes Harry away (the 'shove'). map0_s01 has an extra stateStep-gated fallback in Control_46 (line 8794-8812) that partially covers it there, but the other 5 maps have only the HoverBiteAttack-only path.
    - *Fix:* Make the universal top-level PC bite trigger (air_screamer.c ~line 97/155) recognize ALL damage-dealing attack anims, not just HoverBiteAttack: `isBiteActive = (curStatus==ANIM_STATUS(AirScreamerAnim_HoverBiteAttack,true) || ==StandBiteAttack || ==GlideScratchAttack || ==HoverInjuredBiteAttack)`. Keep the per-swing lockout (s_biteDamaged) and the g_AsBiteCooldown gate so each attack lands once. Better long-term fix matching the reference: instead of a side-channel damage set, restore the vanilla path by populating field_44.field_18/field_C/field_E from the AS body/position when ptr_D48 is NULL (use airScreamer->position + rotation.vy as the cone tip/heading, like the Control_46 _bitePos already does at line 8817), then let func_8008A3E0 do the real cone test + one-hit-per-swing damage. That routes all four anims through the canonical pipeline used by groaner/floatstinger.
    - *Verify:* Core mechanism is REAL and verified. (a) Vanilla cone path break-skipped: line 13614 `if (ptr_D48[1]==NULL) break;` skips func_800805BC (13616) + func_8008A3E0 (13624) for HoverBiteAttack/StandBiteAttack; line 13639 same for GlideScratchAttack skipping 13642-13659. func_8008A3E0 (bodyprog_combat_8008A058.c:328) is the cone-test-and-apply-damage func reading field_44 cone params, so the break genuinely yields zero damage. (b) Top-level fallback line 97 `isBiteActive = (curStatus == ANIM_STATUS(HoverBiteAttack,true))` -- HoverBiteAttack ONLY, verified. (c) Control_46 proximity fallback (8692-8835) and Control_47 are inside `#ifdef MAP0_S01` (8577, 8880), so map0_s01-only -- exactly as claimed. (d) GlideScratchAttack is set in Control_40 which is `#ifndef MAP0_S01` (7810), confirming it is the wild/non-cafe attack that, on those 5 maps, has NO damage fallback -> deals no damage while AS collision cylinder still shoves Harry. CAVEAT / factual error: the defect calls these 'FOUR damage-dealing attack anims' including HoverInjuredBiteAttack(9), and the proposed fix adds it to isBiteActive. That is WRONG -- there are only THREE damage cone anims; line 13393's HoverInjuredBiteAttack case is in a speed-computation switch (returns speeds, ground-height), and func_8008A3E0 is never invoked for anim 9. So the bug is confirmed but the fix's anim list should be HoverBiteAttack/StandBiteAttack/GlideScratchAttack, not include HoverInjuredBiteAttack.
- **[C] high/damage_dealing** (conf medium) `C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/air_screamer.c:13737`
    - The PC collision fallback hardcodes the chara-to-chara push radius cylinder.field_2 = Q12(1.5f). Per the groaner reference, field_2 (@offset 0x2 in s_CharaCylinder) is the chara-to-chara hit/push radius. The vanilla polygon path computes field_2 from bone geometry (line 13803: `airScreamer->collision.cylinder.field_2 = var_t1;` where var_t1 = SquareRoot12 of the body extent, much smaller). The 311f49281 fix correctly reduced cylinder.radius (geometry collision) to the real ~0.6 value, but left field_2 hardcoded at 1.5 -- this oversized push radius is exactly the 'shove Harry across the room' symptom: the AS pushes Harry from 1.5 units away even when its attack deals no damage. This compounds defect #1.
    - *Fix:* Replace the hardcoded `cylinder.field_2 = Q12(1.5f)` at line 13737 with the real per-keyframe extent. The simplest data-driven value available in the NULL-ptr fallback is field_D70[sp10][1] (same source the radius now uses, ~0.6) or compute a small fixed value matching the body size; do NOT leave 1.5. This shrinks the shove bubble to match PSX so the AS must actually reach Harry to push him.
    - *Verify:* Verified at lines 13736-13737: the NULL-ptr fallback sets `cylinder.radius = field_D70[sp10][1]` (the data-driven ~0.6 from the 311f49281 fix) but immediately hardcodes `cylinder.field_2 = Q12(1.5f)`. The vanilla path (line 13803) sets `cylinder.field_2 = var_t1` where var_t1 = SquareRoot12(body XZ extent) >> 1 (lines 13768-13800) -- geometry-derived and much smaller. Header chara.h:552 documents field_2 as the chara-to-chara collision radius. Its consumer bodyprog_combat_8008A058.c:1935-1946 offsets the collision test position by field_2*sin/cos, so a larger field_2 enlarges the AS separation footprint -> earlier/harder push, consistent with the 'shove' symptom. Player's own field_2 is ~0.23-0.5 throughout player_control.c, making 1.5 clearly oversized. The fix's reasoning (use field_D70[sp10][1] like the radius, or a small body-derived value) is sound. Note: this is the same magnitude (1.5) the adjacent comment already calls a ~2.5x oversized bubble for the radius, so leaving field_2 at 1.5 is an internally-inconsistent leftover. Confirmed.
- **[C] low/anim** (conf medium) `C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/air_screamer.c:8692-8835`
    - Heavy duplicated/competing PC fallback logic. Three separate ad-hoc damage mechanisms coexist: (a) top-level Update path lines 93-178 (HoverBiteAttack only, all maps), (b) Control_46 proximity fallback lines 8794-8812 (map0_s01, stateStep-gated, distSqr<9, kf 8-22), and (c) func_8008A0E4 call at line 8821 (map0_s01). They use different distance thresholds (16 vs 9 vs cone), different keyframe windows, and overlapping lockouts (s_biteDamaged vs _asDamagedThisSwoop2 vs HP-delta lockout). This is fragile and the inconsistent gating contributes to attacks feeling unreliable. Not a crash, but it obscures the real fix and risks double/missed hits between paths.
    - *Fix:* After implementing the canonical fix in defect #1 (populate cone params from position when ptr_D48 NULL, route all attacks through func_8008A3E0 with the shared one-hit-per-swing guard), delete the redundant proximity/direct-damage hacks (top-level lines 118-178 damage block, Control_46 lines 8794-8813) so there is a single damage path matching the groaner/floatstinger reference. Keep g_AsBiteCooldown only if PSX-authentic cadence requires it.
    - *Verify:* Verified the three coexisting damage mechanisms and three damage-set sites. (a) Top-level Update path line 164 sets pl->damage.amount with distSqr<Q12(16.0f) and s_biteDamaged lockout (155-178). (b) Control_46 proximity fallback line 8806 sets damage with distSqr<Q12(9.0f) and kf 8-22 plus _asDamagedThisSwoop2 + HP-delta lockout (8794-8812). (c) Control_46 line 8821 calls func_8008A0E4 (cone dispatcher). Distance thresholds genuinely differ (16 vs 9 vs cone) and lockouts differ (s_biteDamaged vs _asDamagedThisSwoop2 vs _asPrevHarryHP2 HP-delta), exactly as claimed. Cited comments verified: line 8693 'the only enemy that doesn't call func_8008A0E4', line 8738-8744 'vanilla cone-collision frequently fails... f44.f0 stays at 3'. This is real fragility/duplication. Severity is correctly rated low (not a crash; cleanup). Confirmed as an accurate description; the proposed consolidation is reasonable but depends on first landing #1's canonical-path fix and should be done carefully since the map0_s01 paths currently are the only working damage on that map.

### night_flutter (id 3) — role: regular
- **Status:** NightFlutter (charaId 3) is the red Air Screamer variant and is NOT a distinct enemy implementation: both slot 2 (AirScreamer) and slot 3 (NightFlutter) in map4_s02_header.c:135 and map6_s00_header.c:136 map to AirScreamer_Update. It shares 100% of code/data with air_screamer.c (LIVE via 6 per-map Chara_AirScreamer.c wrappers; NightFlutter hosted by map4_s02 + map6_s00). The only NightFlutter-specific divergence in the whole file is its health-variation constant (NIGHT_FLUTTER_RAND_MAX=300 vs AIR_SCREAMER_RAND_MAX=100, air_screamer.c:544-551). Therefore NightFlutter inherits every air_screamer fix AND every air_screamer band-aid. Data infrastructure is in good shape: real non-zero anim table AIR_SCREAMER_ANIM_INFOS[56] is defined in air_screamer.c itself (line 13914) and registered via ModelAnim_AnimInfoSet (line 670) - no separate *AnimInfos_Init in main_pc.c is needed (correct, like the Stalker pattern). AI rodata is fully extracted from disc as g_AsRodataPsxRaw[0xD78] in data_stubs.c (NOT a zero-stub) and reformatted to PC layout via AsRodata_Reformat() called at main_pc.c:521. The known air_screamer fixes are PRESENT: hit-radius orb fix (real field_D70[sp10][1] instead of hardcoded 1.5, line 13736), Ray_TraceQuery merge fix (line 824), x64-safe Chara_NpcIdxGet (loop-based pointer subtraction). FPS gating is mostly safe (timers g_DeltaTime-decremented, g_AsBiteCooldown g_DeltaTime-decremented). The MAIN concern is damage-DEALING: it is NOT the clean shared func_8008A0E4 path; instead it carries THREE overlapping PC-fallback damage mechanisms with independent lockouts that can stack in one frame - this is the residual root of the user-reported 'air_screamer barely attacks and just shoves Harry' symptom area, now over-corrected into a redundant/fragile state rather than the canonical once-per-swing pattern.
- Live: ['C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/air_screamer.c', 'C:/Claude/silenthill/silent-hill-decomp/pc_port/src/as_rodata_reformat.c', 'C:/Claude/silenthill/silent-hill-decomp/pc_port/src/stubs/data_stubs.c (g_AsRodataPsxRaw[0xD78])', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map4_s02/Chara_AirScreamer.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map6_s00/Chara_AirScreamer.c']
- **Missing fixes:** ['canonical_once_per_swing_damage_via_shared_func_8008A0E4_only (AS uses 3 overlapping fallback paths instead of the single shared one-hit-per-swing bitmask path used by groaner/floatstinger)']
- **[C] medium/damage_dealing** (conf medium) `C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/air_screamer.c:113-178 (universal bite block in AirScreamer_Update) AND :8704-8834 (swoop block in Ai_AirScreamer_Control_46)`
    - Three overlapping PC-fallback damage mechanisms can apply in the SAME frame with independent lockouts. (1) Control_46 swoop block sets pl->damage.amount = Q12(15.0f) (line 8806, gated !_asDamagedThisSwoop2 && distSqr<Q12(9) && kf 8-22), then (2) unconditionally calls func_8008A0E4 (line 8821) which does damage.amount += ... internally, then (3) the universal Update block sets pl->damage.amount = Q12(15.0f) AGAIN (line 164, gated isBiteActive && !s_biteDamaged && g_AsBiteCooldown==0, distSqr<Q12(16)). The lockouts (_asDamagedThisSwoop2, s_biteDamaged, g_AsBiteCooldown) are independent and do not cross-suppress, so the player can be hit by the swoop block AND the universal block in one frame. Because the fallbacks use '=' assignment and func_8008A0E4 uses '+=', the net per-frame damage to Player_ReceiveDamage is order/path-dependent and not deterministic. This is the over-corrected remnant of the 'AS barely attacks/just shoves' bug: rather than the clean shared once-per-swing path (groaner/floatstinger), AS now has a fragile triple path.
    - *Fix:* Collapse to ONE damage path matching the floatstinger reference: keep the func_8008A0E4 call (line 8821) as the single damage emitter so the shared once-per-swing bitmask guard (bodyprog_combat_8008A058.c:1522-1559) governs it, and verify Unk69's WEAPON_ATTACK damage row (D_800AD4C8[Unk69].field_4) is non-zero so it deals real HP loss (not just knockback). Then DELETE the two direct pl->damage.amount = Q12(15.0f) fallback writes (lines 164 and 8806) and their ad-hoc lockouts. If the cone genuinely cannot land on PC (the comment at 8738 claims f44.f0 stays at 3), fix the cone-collision/field_44.field_0 sentinel rather than bolting on parallel proximity writes. At minimum, if the fallback must stay, unify the two fallback paths under a SINGLE shared lockout so they cannot both fire in one frame.
    - *Verify:* Re-verified all factual assertions. air_screamer.c:33 AirScreamer_Update calls Ai_AirScreamer_Control at line 55 (which dispatches to Ai_AirScreamer_Control_46 via the funcptr table at 13990), THEN runs the universal bite block at 72-179 in the SAME frame. Control_46 swoop block (8732-8833): sets pl->damage.amount = Q12(15.0f) at 8806 (gated stateStep 1/2/3 + HoverBiteAttack + kf 8-22 + distSqr<Q12(9), lockout _asDamagedThisSwoop2/HP-delta), then unconditionally calls func_8008A0E4 at 8821 which internally does target->damage.amount += damageAmount (bodyprog_combat_8008A058.c:1573, itself gated by the once-per-swing bitmask 1553-1557). Universal block (155-178): sets pl->damage.amount = Q12(15.0f) again at 164, gated isBiteActive && !s_biteDamaged && g_AsBiteCooldown==0 && distSqr<Q12(16) with NO keyframe gate (the kf 8-22 check at 142 is logging-only). The three lockouts are genuinely independent: swoop block never sets s_biteDamaged or arms g_AsBiteCooldown; universal block never checks _asDamagedThisSwoop2. So during a HoverBiteAttack swoop frame with Harry inside distSqr<9 (which is also <16), kf 8-22, cooldown 0 and neither lockout latched, BOTH blocks' gates pass in the same frame -> no cross-suppression, exactly as claimed. The cited comment at 8693-8702 matches verbatim ('The AS file is the only enemy that doesn't call func_8008A0E4... Don't gate on the return value... Just rely on the side-effect path'). field_4 for Unk69 (D_800AD4C8[69], item_screens_3.c:147 = {Q12(0.1f), Q12(0.0f), 0x0006,...}) is 6, non-zero per the struct layout in bodyprog.h:206, so the proposed canonical single-path fix is feasible. Caveat (does not refute): because the universal write uses '=' and runs last, the per-frame damage.amount settles to 15.0, not 30 -> 'non-deterministic net damage in one frame' is slightly overstated since intra-frame order is fixed. But the real defect (fragile triple damage path with independent lockouts deviating from the clean shared once-per-swing pattern used by groaner/floatstinger) is fully borne out by the code.
- **[C] low/damage_dealing** (conf high) `C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/air_screamer.c:8693-8835 (entire PC swoop damage block is inside an #ifdef SH_PC_PORT ... #endif at line 8835), plus the parallel block 72-179`
    - The two large PC fallback damage blocks are heavily annotated diagnostic/iterative code (static _swoopLogN, [AS] SH_DBG traces at 135/144/169/175, multiple superseded-attempt comments). This is debugging scaffolding shipped in the hot path; it indicates the AS attack was never settled to the canonical pattern. Not a correctness bug by itself but a maintenance/clarity liability and a sign the once-per-swing model was bypassed rather than fixed.
    - *Fix:* Once the single-path fix above is applied and confirmed in a real fight (the [AS] traces will show whether func_8008A0E4 lands), remove the diagnostic statics/logging and the superseded-attempt comment blocks, leaving only the canonical emitter + SH_DBG-gated diagnostics if still needed.
    - *Verify:* All cited diagnostic scaffolding verified present in the live hot path. air_screamer.c:8705 'static int _swoopLogN = 0;'; 8714-8716 'static u8 _asPrevSwoopStep_v2 = 0; static u8 _asDamagedThisSwoop2 = 0; static s32 _asPrevHarryHP2 = -1;'; SH_DBG('[AS] ...') traces at 135 (bite-anim start), 144 (in-range but NOT biting), 169 (BITE HIT), 175 (bite anim active but OUT OF RANGE), plus s_loggedBite/s_loggedNear logging statics at 131-132 and superseded-attempt comment blocks at 72-92 and 8692-8716 documenting multiple prior iterations. The entire swoop block is inside #ifdef SH_PC_PORT...#endif (8603/8835) and the universal block under #ifdef SH_PC_PORT (33/180). This is iterative debugging scaffolding shipped in the per-frame attack path, confirming the AS/night_flutter attack was never settled to the canonical once-per-swing model -- a maintenance/clarity liability, exactly as the (correctly self-described 'not a correctness bug by itself') claim states. Evidence holds exactly.

### wormhead (id 5) — role: regular
- **Status:** CLEAN â€” no defects. Wormhead (charaId 5) is a Groaner reskin and follows the grey_child shared-sibling pattern correctly. It legitimately has NO wormhead.c / wormhead_anim_infos.c / wormhead_rodata.inc because: (1) both hosting maps map charaUpdateFuncs[5] (Chara_Wormhead) to Groaner_Update (map4_s02_header.c:135 and map6_s00_header.c:136 â€” array index 5 = the 6th slot = second Groaner_Update entry), (2) Groaner_Update unconditionally binds GROANER_ANIM_INFOS (groaner.c:110) with no charaId branching, so Wormhead reuses Groaner's full anim/AI/collision/combat data, and (3) the two enemies differ ONLY in their model/texture assets â€” chara_data_info.c:11-12 gives Groaner FILE_CHARA_DOG_ILM/TIM and Wormhead FILE_CHARA_DG2_ILM/TIM, but BOTH use FILE_ANIM_DOG_ANM (same animation set). The blood-type table treats them identically (bodyprog_combat_8008A058.c:1627-1630, case Chara_Groaner/case Chara_Wormhead -> var_a3=6). USED/LIVE: Wormhead is actively spawned (map4_s02/chara_spawns.h:17-18 has 2 spawns; map6_s00/chara_spawns.h:29-37 has ~9 spawns) and listed in both maps' charaGroupIds. groaner.c is LIVE in both maps via Chara_Groaner.c wrappers that #include characters/groaner.c (pulling in groaner_rodata.inc into each DLL TU). All Groaner reference fixes (complete data, once-per-swing FPS-safe damage, real-data hit radius, floor clamp, x64 eval-order/NULL guards) are inherited and present. No action needed for Wormhead specifically â€” its correctness is identical to and fully covered by the known-good Groaner reference enemy.
- Live: ['C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/groaner.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/groaner_rodata.inc', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map4_s02/Chara_Groaner.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map6_s00/Chara_Groaner.c', 'C:/Claude/silenthill/silent-hill-decomp/pc_port/src/groaner_anim_infos.c']
- No defects found.

### larval_stalker (id 6) — role: regular
- **Status:** CLEAN / VERIFIED COMPLETE. larval_stalker (charaId 6) is a fully-faithful, complete port matching the groaner/stalker reference pattern across all 7 audit dimensions. It is LIVE in 3 maps (map1_s00, map1_s01, map6_s04) via per-map Chara_LarvalStalker.c wrappers, registered at the correct Chara_LarvalStalker slot (index 6) in each header's charaUpdateFuncs. All three data pillars are in place: real anim_infos.c + Init in main_pc.c, real rodata.inc (collision-zerostub fix present), and proper dual-location data split (anim infos in exe, AI/collision keyframes in same DLL TU). No high-FPS, x64-pointer, damage, anim, floor, or OT defects found. The only finding is a dead leftover diagnostic no-op block (low severity, cosmetic). Note: it is NOT in the AnimInfos_Init/_rodata 'broken enemy' class and matches the known-good reference shape.
- Live: ['C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/larval_stalker.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/larval_stalker_rodata.inc', 'C:/Claude/silenthill/silent-hill-decomp/pc_port/src/larval_stalker_anim_infos.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map1_s00/Chara_LarvalStalker.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map1_s01/Chara_LarvalStalker.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map6_s04/Chara_LarvalStalker.c']
- **[C] low/other** (conf high) `C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/larval_stalker.c:874-884`
    - Leftover temporary PC-port diagnostic block inside sharedFunc_800D17BC_1_s00 (damage-taking entry). It reads larvalStalker->damage.amount into a static s_prevAmt edge tracker but does NOTHING with it (the SH_DBG log that was once here is gone). It is a dead no-op, not a functional bug, but it is dead code the comment itself marks 'temporary'.
    - *Fix:* Delete the dead diagnostic block (lines 874-884). If attack-reach diagnostics are still wanted, replace with an actual SH_DBG(...) call rather than a no-op static tracker.
    - *Verify:* Re-read larval_stalker.c:867-884. The cited block exists verbatim inside sharedFunc_800D17BC_1_s00 (the damage-taking entry, right before the 'damage.amount <= 0 || health <= 0' early-return at line 886). It declares 'static s32 s_prevAmt = 0', reads 'amt = (s32)larvalStalker->damage.amount', and on the edge (amt != s_prevAmt) only assigns 's_prevAmt = amt' with no SH_DBG or other side effect. The original comment explicitly says 'DIAG (temporary)'. The file is LIVE/compiled: map1_s00/map1_s01/map6_s04 each have a Chara_LarvalStalker.c wrapper, and map1_s00/Chara_LarvalStalker.c line 14 does #include "../src/maps/characters/larval_stalker.c". So this is genuine dead no-op code in a compiled file, correctly classified as other/low, not a functional bug. Deletion is warranted.

### stalker (id 7) — role: regular
- **Status:** CLEAN. The Stalker (charaId 7, also the LIVE host for GreyChild=8 and Mumbler=9, all three routed to Stalker_Update) is one of the most complete and faithful enemy ports in the codebase. All seven audit dimensions (A-G) pass. No real defects found. The two known PC-port fixes specific to this enemy family (stalker_rodata.inc collision keyframes + g_DeltaTime==0 div guard) are both PRESENT and must be preserved. Only one cosmetic, benign type mismatch noted (low severity, no functional impact).</parameter>
<parameter name="live_files">["C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/stalker.c", "C:/Claude/silenthill/silent-hill-decomp/src/maps/map0_s00/Chara_Stalker.c", "C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/stalker_rodata.inc", "C:/Claude/silenthill/silent-hill-decomp/src/maps/map0_s00/map0_s00_anim_info.c", "C:/Claude/silenthill/silent-hill-decomp/src/maps/map0_s00/map0_s00_header.c"]
- **[C] low/data_zerostub** (conf high) `C:/Claude/silenthill/silent-hill-decomp/pc_port/src/stubs/data_stubs.c:1417-1426 vs C:/Claude/silenthill/silent-hill-decomp/include/maps/shared.h:233-245`
    - Type mismatch between declaration and definition for the stalker AI scratch/tuning symbols. shared.h declares them as scalars/typed arrays (e.g. `extern q19_12 sharedData_800E3A20_0_s00;` line 233, `extern s32 sharedData_800E39EC_0_s00[];` line 242), but data_stubs.c DEFINES every one of them as `u8 sharedData_800Exxxx_0_s00[256] = {0};`. This is functionally BENIGN (not a real bug): (a) these are runtime scratch state (player-velocity ring buffer sharedData_800E39EC/3A0C, smoothed target X/Z 3A18/3A1C, and the difficulty health/regen constants 3A20/3A24/3A28/3A2C) that Stalker_Update and Ai_Stalker_ControlUpdate WRITE every frame before reading, so the {0} init never matters; (b) the 256-byte definition is LARGER than any scalar access through the typed extern, so the type mismatch over-allocates rather than truncating -- no adjacent-symbol overrun. Flagging only because a u8[256] zero-stub form is the same surface pattern as a real broken data stub, and it is easy to misread on a future audit.
    - *Fix:* Optional cleanup only (no behavior change): give these their real scalar/array types in data_stubs.c to match shared.h, e.g. `q19_12 sharedData_800E3A20_0_s00 = 0;`, `s32 sharedData_800E39EC_0_s00[5] = {0};`, `u16 sharedData_800E3A0C_0_s00[5] = {0};` (the ring buffers are indexed [0..4] in Ai_Stalker_ControlUpdate). Do NOT treat as a data-extraction gap -- there is no PSX rodata to extract here; these are zero-initialized runtime state by design. Leave as-is if a low-risk audit is preferred.
    - *Verify:* Every cited fact verified. data_stubs.c:1419-1426 define sharedData_800E39EC/3A0C/3A18/3A1C/3A20/3A24/3A28/3A2C as 'u8 ...[256] = {0}' (read lines 1417-1426). shared.h:233-245 declare them as scalars/typed arrays: 3A20 'extern q19_12 ... // Health.', 39EC 'extern s32 ...[]', 3A0C 'extern u16 ...[]'. stalker.c:39 writes 3A20 as a scalar (= Q12(350.0f)). The type mismatch is real but BENIGN as claimed: (a) the 256-byte u8 definition over-allocates vs every typed access (scalar reads/writes and ring-buffer indices [0..4]), so no truncation and no adjacent-symbol overrun; (b) the values are runtime state written before read each frame -- difficulty constants 3A20-3A2C written at Stalker_Update top (lines 39-57) before any read; ring buffers 39EC/3A0C shift-in at lines 573-577 before reads at 606-619, indexed [0..4] confirming the proposed [5] sizing. stalker.c is LIVE (8 Chara_Stalker.c wrappers in real src/maps tree), and these symbols are referenced ONLY in stalker.c -- they are runtime scratch/tuning, not rodata, so the claim's 'no PSX rodata to extract' is correct. Confirmed as a real-but-benign type-mismatch surface pattern warranting at most optional cosmetic cleanup, not a behavioral fix.

### mumbler (id 9) — role: regular
- **Status:** CLEAN. No defects found. The Mumbler (Chara_Mumbler, id 9) is the PAL/NTSC-J censorship skin of the Grey Child and a behavior-identical member of the Stalker family. It is NOT a distinct implementation: every hosting map routes charaUpdateFuncs[9] -> Stalker_Update, all AI/anim/damage/collision logic lives in the known-good shared stalker.c (LIVE), the anim table is the shared exe-resident STALKER_ANIM_INFOS, and the collision keyframe rodata is the shared, fully-extracted stalker_rodata.inc. All PC-port fix patterns that apply (data extraction, once-per-swing combat gate, FPS-safe ranges, div-by-zero guard) are present and inherited from the Stalker shared code. The only Mumbler-specific PC code is the region-patch that points Grey Child's model/texture at the Mumbler assets on EUR discs (chara_data_info.c), which is asset-only and correct.
- Live: ['C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/stalker.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/stalker_rodata.inc', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map0_s00/map0_s00_anim_info.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map6_s03/Chara_Stalker.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map5_s02/characters.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map6_s03/map6_s03_header.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map5_s02/map5_s02_header.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map0_s00/map0_s00_header.c', 'C:/Claude/silenthill/silent-hill-decomp/src/bodyprog/sys/chara_data_info.c']
- No defects found.

### hanged_scratcher (id 10) — role: regular
- **Status:** CLEAN - fully-faithful complete port. All four reference pillars present (real anim_infos.c[54] + registered Init, real rodata.inc included in-TU, FPS-safe combat via func_8008A0E4 range-gated windows, data-driven damage/floor). No residual zero-stubs, no x64/ptr defects, no high-FPS == keyframe gates. Two LOW-severity items only: a PSX-faithful collision-keyframe end-index over-read (likely never reached; clamp only if a capture shows a glitch) and a cosmetic scratch-var type mismatch (benign). Matches the groaner/stalker known-good pattern.
- Live: ['C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/hanged_scratcher.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map5_s00/Chara_HangedScratcher.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map6_s03/Chara_HangedScratcher.c', 'C:/Claude/silenthill/silent-hill-decomp/pc_port/src/hanged_scratcher_anim_infos.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/hanged_scratcher_rodata.inc']
- **[C] low/collision_floor** (conf low) `C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/hanged_scratcher.c:1344-1345 (also 1460-1461, 1565-1566)`
    - Collision-keyframe array index can reach last element + 1 at the exact end keyframe. For Anim_4 active (status 9), keyframe range is 98..115 (seed start=98 end=115); relKeyframeIdx = animKeyframeIdx - 98 can reach 17, and the code reads sharedData_800D9500_5_s00[relKeyframeIdx] and [relKeyframeIdx + 1] = [17] and [18], while the array has only 17 entries (0..16). Same shape at Anim_16 true (line 1460-1461, base 244 into the [22] array sharedData_800D9820) and Anim_5 true (line 1565-1566, base 116 into the [20] array sharedData_800D9B2C). On PSX adjacent overlay memory absorbed the read; on PC these are separate globals so the index reads past the array.
    - *Fix:* PSX-faithful arithmetic; in practice Anim_PlaybackOnce transitions on link-status before anim.time holds at the exact end keyframe, so the OOB index is generally not reached. If a fight capture ever shows a glitch/AV here, clamp relKeyframeIdx and the +1 index to [0, ARRAY_LEN-1] under #ifdef SH_PC_PORT (twinfeeler OT-clamp precedent). Leave untouched otherwise to preserve clean decomp.
    - *Verify:* Arithmetic verified exactly. Array sizes from hanged_scratcher_rodata.inc: [17], [22], [20]. Anim end keyframes from the header's anim-infos table: Anim_4 true ends at 115 (98 base) -> relKeyframeIdx max 17, reads [17] and [18] past a [17] array; Anim_16 true ends at 266 (244 base) -> idx 22, reads [22]/[23] past a [22] array; Anim_5 true ends at 135 (116 base) -> idx 19, reads [19]/[20] past a [20] array. So each site can read last+1 (and +2 for the +1 index) at the exact end keyframe. The index source is animKeyframeIdx = FP_FROM(scratcher->model.anim.time, Q12_SHIFT) in HangedScratcher_CollisionUpdate, driven by anim.time, not bounded by the link-status transition. Crucially the PC port already changed Anim_PlaybackStateGet to a range form (keyframeIdx >= endKeyframeIdx, chara_util.c:218-220) precisely because PC delta-time can step PAST the exact end keyframe at high FPS; that makes the OOB read genuinely reachable on PC (a 1-2 element overread into a separate global, since on PC these are distinct symbols, not one contiguous overlay). The defect is real and accurately stated. The proposed disposition (leave clean decomp untouched, clamp under SH_PC_PORT only if a capture shows a glitch/AV, per twinfeeler OT-clamp precedent) is sound for a low-severity latent overread that reads into adjacent valid globals rather than wild memory.
- **[C] low/data_zerostub** (conf high) `C:/Claude/silenthill/silent-hill-decomp/pc_port/src/stubs/data_stubs.c:1336 vs include/maps/characters/hanged_scratcher.h:131`
    - Type mismatch on scratch global sharedData_800DE28C_5_s00: header declares 'extern q19_12 sharedData_800DE28C_5_s00;' (4-byte scalar) but data_stubs.c defines 'u8 sharedData_800DE28C_5_s00[256] = {0};'. This is a per-frame working/scratch variable (written with Q12(0/8/10/12/15) in Control_8/_11, read into anim duration.constant), NOT extracted disc data, so size/value is functionally irrelevant; the u8[256] over-provisions storage and code accesses it through the q19_12 extern as a 4-byte read. The non-compiled duplicate in build_gen/extracted_data/map5_s00_extracted_data.c is not referenced by CMakeLists (only MAP0_S00_EXTRACTED), so no duplicate-symbol conflict.
    - *Fix:* Cosmetic: change data_stubs.c to 'q19_12 sharedData_800DE28C_5_s00 = Q12(0.0f);' to match the header type. No behavioral effect; safe to leave as-is since it is a scratch var with sufficient storage.
    - *Verify:* Verified exactly. data_stubs.c:1336 defines 'u8 sharedData_800DE28C_5_s00[256] = {0};' while include/maps/characters/hanged_scratcher.h:131 declares 'extern q19_12 sharedData_800DE28C_5_s00;' (4-byte scalar) -- a genuine type mismatch. The variable is a per-frame scratch/working var: written with Q12(0/8/10/12/15) in hanged_scratcher.c lines 748/752/757/835/839/854 and read into curAnimInfo.duration.constant at line 1261, all through the q19_12 extern (a 4-byte access). It is NOT extracted disc data, so the over-provisioned u8[256] is harmless storage; only the first 4 bytes are touched. Verified no duplicate-symbol conflict: CMakeLists.txt only references MAP0_S00_EXTRACTED (lines 134-136); map5_s00_extracted_data.c and map6_s03_extracted_data.c (which also define this symbol) are NOT added to the build. The defect, its low severity, and the cosmetic-only fix are all accurate.

### creeper (id 11) — role: regular
- **Status:** CLEAN on dimensions A (data), B (x64 pointer), C (damage-taking), D (damage-dealing), E (anim), G (fps-gate). The creeper port is faithful and complete: real anim infos + registered Init, full extracted rodata keyframes, no residual zero-stubs (sharedData_800E57CC is a legit runtime scratch counter, not a data table), correct shared once-per-swing combat, all-range keyframe gates, dt-scaled timing. The ONLY issue touching the user's reported symptom (dimension F) is NOT a creeper defect -- the creeper's own floor/gravity logic is correct. The 'fall through and reappear' is the documented SHARED Collision_CharaCollisionSetup NULL-IPD fallback (collision.c:487-491, default groundHeight Q12(8.0) when no collision chunk is streamed under the NPC), which manifests for sewer creepers because many fast small enemies straddle chunk-load boundaries. Fix belongs in IPD/streaming coverage, not creeper.c.
- Live: ['C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/creeper.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/creeper_rodata.inc', 'C:/Claude/silenthill/silent-hill-decomp/pc_port/src/creeper_anim_infos.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map1_s02/Chara_Creeper.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map1_s03/Chara_Creeper.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map3_s01/Chara_Creeper.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map3_s02/Chara_Creeper.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map5_s00/Chara_Creeper.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map3_s05/map3_s05.c']
- **[U] medium/collision_floor** (conf medium) `C:/Claude/silenthill/silent-hill-decomp/src/bodyprog/collision/collision.c:487-491 (shared infra, exercised by creeper via creeper.c:702 func_8005CB20)`
    - The user-reported 'sewer monsters fall through the floor and reappear' is NOT a creeper-specific defect. Creeper's own floor logic is faithful: sharedFunc_800D983C_1_s02 (creeper.c:691-716) accumulates gravity via the frame-time-scaled, 30fps-capped g_GravitySpeed (>>2 for the lighter insect), then calls func_8005CB20 which resolves motion through Collision_WallDetect and clamps position.vy to surface.groundHeight with fallSpeed=0 (bodyprog_combat_8005BF38.c:154-158) -- all dt-scaled and FPS-safe. The fall-through originates in the SHARED Collision_CharaCollisionSetup: when Ipd_CollisionDataGet(chara->position.vx, vz) returns NULL (no IPD collision chunk loaded under the creeper's position), it calls Collision_DefaultResultSet(...,Q12(8.0f)) and returns -- giving a default groundHeight of Q12(8.0). A creeper standing/moving over an area whose IPD collision chunk is not currently streamed gets groundHeight=8.0, drops (fallSpeed keeps building since position.vy stays > the bogus floor or the creeper sinks), then snaps back when the chunk reloads and a real groundHeight returns -> the 'fall through and reappear' symptom. Sewers have many small fast-moving creepers spread across chunk boundaries, so they hit the NULL-IPD window more than slower enemies.
    - *Fix:* This is the documented shared fall-through root (MEMORY: 'sewer fall-through = chunk not loaded under the enemy'). Fix belongs in the streaming/IPD-coverage layer, not creeper.c: ensure the collision chunk under every active NPC's position is loaded (extend the load window to NPC positions, not just the camera/player), OR, as a localized safety, when Ipd_CollisionDataGet returns NULL for a grounded NPC, hold the chara's previous groundHeight / skip the gravity step for that frame instead of dropping it to Q12(8.0). Do NOT special-case creeper -- verify the sewer maps (map1_s02/s03, map3_s01/s02) actually load IPD collision chunks across the full creeper roam area. Recommend confirming with an [IPD]/groundHeight probe at creeper.c:702 whether collResult.surface.groundHeight==Q12(8.0) during the observed drops before changing streaming logic.
    - *Verify:* The cited code is accurate: collision.c:487-491 reads exactly as quoted -- when Ipd_CollisionDataGet(chara->position.vx, vz)==NULL it calls Collision_DefaultResultSet(...,Q12(8.0f)) and returns true, and Collision_DefaultResultSet (collision.c:532-542) does set surface.groundHeight = the passed Q12(8.0). The downstream floor clamp is also as described: func_8005CB20 (bodyprog_combat_8005BF38.c:154-158) does 'if (position.vy > groundHeight){ position.vy=groundHeight; fallSpeed=0; }', and creeper.c:691 accumulates gravity via g_GravitySpeed>>2 then calls func_8005CB20 at creeper.c:702. The mechanism is physically sound given SH's +Y-down convention: with a creeper at vy~=0 over real floor, a NULL IPD chunk yields groundHeight=8.0; since 0 > 8.0 is false no clamp fires, gravity keeps building fallSpeed and vy sinks toward 8.0 (below the real floor), then snaps back when the chunk reloads -- a real 'fall through and reappear' path. HOWEVER: (1) lines 487-491 are plain original decompiled PSX code, NOT inside any #ifdef SH_PC_PORT block (nearest PC blocks are at 363 and 515), so this is faithful PSX behavior, and PSX did not exhibit this sewer fall-through -- implying the true cause is a PC-side difference in the streaming/IPD load window that was NOT demonstrated or cited. (2) The defect explicitly concedes creeper.c is faithful and pins blame on shared streaming infra, and its own proposed action is to 'verify the sewer maps actually load IPD collision chunks' and to 'confirm with an [IPD]/groundHeight probe whether groundHeight==Q12(8.0) during the observed drops BEFORE changing streaming logic.' That is a hypothesis requiring runtime confirmation, not a verified defect. No PC regression in the streaming layer is shown. The citation is correct and the mechanism is plausible, but neither the actual triggering of the NULL window during the drops nor a concrete PC streaming bug can be established by static reading -- it needs the game running. Hence uncertain rather than confirmed or refuted.

### romper (id 12) — role: regular
- **Status:** CLEAN (near-reference quality). Romper is a fully-faithful, well-ported regular enemy matching the groaner reference pattern: real anim_infos.c + registered Init, real rodata.inc dispatch table, real ROM-extracted collision/movement data delivered via the per-map extracted_data DLL files (the EXE data_stubs.c zero-stubs are correctly shadowed at runtime by the DLL copies). Damage taking/dealing, floor collision, x64 pointer safety, and struct layout are all clean. The ONLY concern is two exact-keyframe FPS gates around the jump-lunge launch (romper.c:962 and 976-977) which COULD skip at high FPS; both are low/medium confidence (the start-snap and integer-floor window plus existing flag latches likely mitigate them) and warrant a fight capture to confirm before changing, not a blind fix."
- Live: ['C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/romper.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map4_s02/Chara_Romper.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/romper_rodata.inc', 'C:/Claude/silenthill/silent-hill-decomp/pc_port/src/romper_anim_infos.c', 'C:/Claude/silenthill/silent-hill-decomp/pc_port/build_gen/extracted_data/map4_s02_extracted_data.c']
- **Missing fixes:** ['bone_count: not applicable - romper has no per-NPC bone-overflow buffer concern (uses standard boneCoords path)', 'OT_clamp: not applicable - romper does not emit otz-derived OT bucket indices (no large animated-geometry draw markers like floatstinger/twinfeeler); collision keyframe array indices use modulo wrap (%9/%16/%22) and clamp (>=4?4) within u8[256] capacity']
- **[R] medium/fps_gate** (conf low) `C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/romper.c:962`
    - Jump-launch trigger uses exact fixed-point equality `if (romper->model.anim.time == Q12(2.0f))` inside the RunToJump,true branch. Anim_PlaybackOnce (bodyprog_anim_800445A4.c:300) advances anim.time by `+= timestep` continuously and only snaps to EXACT integer values at the anim START (newTime <= startTime -> newTime = startTime, line 312-314) or END. Whether 2.0 is hit depends on the start-snap landing exactly on keyframe 2. If the active variant's startKeyframeIdx is not 2 (so 2.0 is an intermediate keyframe), at >30 FPS the small fractional timesteps can step over exactly Q12(2.0) and the launch block (sets romper->fallSpeed/moveSpeed for the lunge, line 968/973) never runs -> the Romper's signature jump-lunge attack fails to launch.
    - *Fix:* If verification shows 2.0 is not a guaranteed start-snap, convert to a range/edge-trigger: gate the launch on `ANIM_TIME_RANGE_CHECK(romper->model.anim.time, 2, 3)` (or a RomperFlag latch like RomperFlag_9 used just below) so it fires exactly once when anim.time first reaches keyframe 2, regardless of FPS. Confirm against PSX behavior first since the start-snap may already make `==2.0` reliable.
    - *Verify:* The defect hinges on 'if the active variant's startKeyframeIdx is not 2'. The extracted table (pc_port/src/romper_anim_infos.c, seed for status 0x05 which decodes to ANIM_STATUS(RomperAnim_RunToJump,true) via ((2<<1)|1)) has startKeyframeIdx=2, endKeyframeIdx=7 -- startKf IS exactly 2. The preceding blend phase status 0x04 (RomperAnim_RunToJump,false; kind=0 BlendLinear, endKf=2, linkStatus=0x05) on completion sets model->anim.time = Q12(endKeyframeIdx) = Q12(2) (bodyprog_anim_800445A4.c:476) and links to status 0x05. Thus on the first active-phase frame anim.time == Q12(2.0f) is guaranteed, independent of FPS, via the blend-handoff/start-snap. Line 905 (Q12_DIV(anim.time - Q12(2.0f), Q12(3.0f)) over the 2.0->5.0 span) structurally confirms the anim is authored to begin at exactly 2.0. Even Anim_PlaybackOnce's own start-snap (lines 312-316, newTime<=startTime -> startTime) would re-snap to Q12(2) if time ever dipped to/below 2. The == Q12(2.0f) launch trigger fires reliably; the proposed range-fix is unwarranted and the defect's own caveat ('confirm... the start-snap may already make ==2.0 reliable') resolves against it.
- **[R] low/fps_gate** (conf low) `C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/romper.c:976`
    - The jump melee/SFX window uses integer-keyframe equality `FP_FROM(romper->model.anim.time, Q12_SHIFT) == 5 || == 6` to fire the attack (func_8008A0E4 at line 989) and the leap SFX. This is a 2-keyframe window and is latched by RomperFlag_9 (SFX) and by the func_8008A0E4 once-per-swing guard (damage), so it is much more robust than a bare `== exactKf`; but at very high FPS a single timestep could in principle exceed 2 keyframes and skip both 5 and 6, dropping the bite. Lower risk because the integer-floor comparison covers the whole frame where the integer part is 5 or 6.
    - *Fix:* Prefer a range/`>=`-with-latch: gate on `ANIM_TIME_RANGE_CHECK(romper->model.anim.time, 5, 7)` (the existing RomperFlag_9 latch already prevents re-fire). Damage is already once-per-swing-safe via func_8008A0E4; this only hardens the trigger window against frame skips.
    - *Verify:* Cited code (romper.c:976-997) is real and the window is FP_FROM(anim.time)==5 || ==6, i.e. integer-floor over anim.time in [5.0,7.0) -- a full 2-keyframe-wide window, far more robust than a bare ==exactKf. The claim labels this a HIGH-FPS hazard, but the timestep math contradicts that: timestep = Q12_MULT_PRECISE(duration, g_DeltaTime) with duration = Q12(12.0) for status 0x05, giving ~0.4 keyframes/frame at 30 FPS and SMALLER at higher FPS (more samples land inside the window as FPS rises). Skipping the [5,6] window requires a single timestep > 2 keyframes, i.e. roughly 6 FPS -- a LOW-framerate condition, the opposite of the asserted high-FPS risk. Damage is already once-per-swing via func_8008A0E4 and the SFX is latched by RomperFlag_9. The defect self-rates 'low' and 'in principle'; it is not a real high-FPS bug and the failure direction it describes is inverted, so it is refuted as stated.

### split_head (id 14) — role: boss
- **Status:** CLEAN (PSX-faithful) on all gameplay-critical dimensions. All data infrastructure is correctly in place: real non-zero anim_infos.c + registered Init, real rodata.inc dispatch table, and the three read-only head-segment/bone tables (800D5A90/5AB0/5BE0) are extracted non-zero and DLL-local (the data_stubs.c zero-stubs are dead/shadowed for the DLL build, same dual-location pattern as groaner). The e14be74b7 stack-frame-aliasing crash fix is present. Damage-taking and damage-dealing are PSX-faithful and once-per-swing-correct. Only two LOW-severity cosmetic FPS-exact-keyframe gates found (line 386 blood, line 95 SFX), neither affecting damage or state.", "notes": "USER COMPLAINT ('takes too much damage to kill') - ROOT CAUSE ANALYSIS: This is PSX-FAITHFUL behavior, not a port bug. Split Head health = 25500 stored RAW in a q19_12 field (split_head.c:44, original decomp value, verified in git as the actual PSX value; the TODO comments are the decompiler noting health is not Q12-scaled). Damage is converted Q12->raw via damageAmount = FP_FROM(damage.amount, Q12_SHIFT) i.e. >>12 (split_head.c:191). A handgun does D_800AD4C8[Handgun=row32].field_4 = 0x64 = 100 raw per hit (damage.amount = Q12(100) = 409600, >>12 = 100). So 25500/100 = 255 unamplified handgun hits. HOWEVER the boss has a WEAK-POINT mechanic: when SplitHeadFlag_0 is set (Harry positioned in front of the open 'split' head, computed from GTE bone anchors in sharedFunc_800D3388/800D4408 using the now-confirmed-real 800D5A90/5AB0 tables), damage is multiplied <<4 (x16) normal or <<5 (x32) for a Shotgun tap on non-Hard (split_head.c:203-214). With the x16 window: 25500 / (100*16=1600) ~= 16 handgun hits; shotgun in the window is far faster. So the boss is INTENDED to be tanky unless you exploit the weak-point window. If on PC it genuinely 'never dies', the suspect would be the GTE weak-point detection failing to set SplitHeadFlag_0 - but the data it depends on (800D5A90/5AB0/5BE0) is confirmed extracted non-zero and DLL-local, and the GTE path (Vw_CoordHierarchyMatrixCompute + gte_rt in sharedFunc_800D4408) is the same one floatstinger uses successfully. Recommend a live capture: log splitHead->health, splitHeadProps.flags (specifically SplitHeadFlag_0), and the computed damageAmount per hit during a real fight to confirm the x16 window engages on PC. If Flag_0 never sets during the fight, the bone-anchor angle math (sp98[]/sharedData_800D8618 in sharedFunc_800D3388, line 1644+) is the next place to look. No code change recommended without that capture - the data and math read as correct/faithful. BONE-COUNT note: sharedFunc_800D4408 indexes boneCoords up to idx 23; this uses the per-chara g_CharaModelAnimsData[].boneCoords array (sized to the model), NOT the shared g_NpcBoneCoords[18] that overflowed in the Good+ ending - no overflow here."
- Live: ['C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/split_head.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/split_head_rodata.inc', 'C:/Claude/silenthill/silent-hill-decomp/pc_port/src/split_head_anim_infos.c', 'C:/Claude/silenthill/silent-hill-decomp/pc_port/build_gen/extracted_data/map1_s05_extracted_data.c', 'C:/Claude/silenthill/silent-hill-decomp/pc_port/build_gen/extracted_data/map1_s06_extracted_data.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map1_s05/map1_s05.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map1_s06/map1_s06.c']
- **Missing fixes:** ['damage_rate-limit / per-frame intake cap: unlike the floatstinger boss reference (field_108 caps a single accumulation at Q12(300)/Q12(500)), Split Head has NO per-frame damage-intake cap. This is NOT a port defect - it is PSX-faithful (Split Head uses a small raw health pool of 25500 plus a >>12 damage downscale instead of a Q12 health pool + cap). Listed only to note the boss-reference field_108 mechanism does not apply here by design.']
- **[C] low/fps_gate** (conf medium) `C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/split_head.c:386`
    - Exact-equality keyframe gate on a non-combat (blood/particle) effect. `if (FP_FROM(splitHead->model.anim.time, Q12_SHIFT) == 35)` triggers a blood spurt (func_8005F6B0) on the player during the Split Head's lunge/attack. At >30 FPS anim.time advances in fractional Q12 steps, so floor(time)==35 either fires on EVERY frame where the integer part is 35 (repeated blood spawns) or, on a frame hitch / very low FPS, can be skipped entirely if dt jumps the integer part past 35. This is purely cosmetic - the actual attack DAMAGE is registered separately and correctly once-per-attack at line 1716 (Chara_AttackReceivedSet gated by SplitHeadFlag_9). No gameplay impact.
    - *Fix:* Convert to a RANGE/once gate consistent with the rest of the file, e.g. ANIM_TIME_RANGE_CHECK(splitHead->model.anim.time, 35, 36) combined with a once-per-attack latch flag, mirroring the Flag_9 pattern used for the real attack at line 1714. Low priority since it is a visual-only effect.
    - *Verify:* Verified line 386 reads exactly `if (FP_FROM(splitHead->model.anim.time, Q12_SHIFT) == 35)`. FP_FROM(x, shift) is defined as `(x) >> (shift)` (fixed_point.h:39) and Q12_SHIFT=12, so this extracts the integer part of the Q12 anim time and compares to exact value 35. Lines 388-391 then call func_8005F6B0(&player, &unkPos, 2, 7) with randomized positions around the player â€” a cosmetic blood/particle spurt, not damage. At >30 FPS anim.time advances in sub-integer Q12 steps so floor(time)==35 holds across multiple frames (repeated spawns); a frame hitch could skip it. This is the classic == exactKf FPS-gate anti-pattern (fps_gate dimension G). The 'no gameplay impact' sub-claim is also verified: real damage is at line 1716 via Chara_AttackReceivedSet, gated once-per-attack by !(flags & SplitHeadFlag_9) and then setting SplitHeadFlag_9 (line 1718). So the defect is real but correctly classified as cosmetic/low priority. The proposed range+latch fix is reasonable and consistent with the file's other patterns.
- **[C] low/fps_gate** (conf low) `C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/split_head.c:95`
    - Exact-equality `keyframeIdx == 161` inside the footstep/movement SFX emitter sharedFunc_800CF990_1_s05. It is one disjunct in a wider set of `>= && <` range tests for playing positional SFX, and is guarded by a per-zone field_108[] one-shot latch. At high FPS the integer keyframe 161 may be skipped, causing an occasional missed footstep SFX. Cosmetic/audio only, no gameplay or damage impact.
    - *Fix:* Widen the single-frame disjunct to a range (e.g. keyframeIdx >= 161 && keyframeIdx < 162 is identical; better `(keyframeIdx >= 160 && keyframeIdx < 162)`) so a skipped exact frame still triggers the SFX. Very low priority - audio only.
    - *Verify:* Verified lines 94-97: the test is `(keyframeIdx >= 132 && keyframeIdx < 148) || keyframeIdx == 161 || (keyframeIdx >= 172 && keyframeIdx < 185) || (keyframeIdx >= 200 && keyframeIdx < 202)`, guarded by the field_108[1] one-shot latch (lines 99-105). Line 95 is exactly the single-frame disjunct `keyframeIdx == 161` claimed. At high FPS the integer keyframe 161 can be skipped between frames, causing an occasional missed positional SFX â€” audio only, no gameplay/damage impact. Strong corroboration: the immediately following block at lines 112-115 (field_108[2]) already uses `(keyframeIdx >= 160 && keyframeIdx < 162)` â€” exactly the range the proposed fix suggests â€” proving the codebase already prefers ranges for this same keyframe region and that the lone == 161 disjunct is an inconsistency/skip risk. Real but very-low-priority (audio-only) FPS gate (dimension G).

### twinfeeler (id 20) — role: boss
- **Status:** CLEAN with one UNCONFIRMED diagnostic-only concern. The twinfeeler is a two-phase boss and is structurally faithful. All the known PC-port fixes are present (anim extraction + Init, OT clamps x5, sz==0 div guard, FPS-safe g_DeltaTime gating, once-per-swing combat via shared func_8008A0E4 with a dedicated weapon-attack id Unk61, SFX-loop-stop PC safety additions). No NEW defects were found that I can confirm against the reference pattern. The only open item is the user's own hypothesis (PC FPS-shortened kill window on the phase-1 surfaced worm), which the shipped [WORM] probe is designed to measure and which remains UNCONFIRMED - it is not a code defect on read; the timing is g_DeltaTime-based and the 1-hit kill is correct PSX logic.
- Live: ['C:/Claude/silenthill/silent-hill-decomp/src/maps/map4_s03/map4_s03.c', 'C:/Claude/silenthill/silent-hill-decomp/pc_port/src/twinfeeler_anim_infos.c']
- No defects found.

### bloodsucker (id 21) — role: regular
- **Status:** CLEAN â€” no defects found. The bloodsucker is a fully-faithful, complete port. It is one of the cleanest enemies in the codebase: src/maps/characters/bloodsucker.c is BYTE-IDENTICAL to the alpha6 upstream reference (zero SH_PC_PORT blocks in the .c), so the game logic is untouched clean decomp. All PC-port work is correctly external: the real anim_infos.c with registered Init, replacing the former zero-stub. Audited across all dimensions A-G; nothing to fix.</parameter>
<parameter name="notes">ROLE: The bloodsucker is a stationary ENVIRONMENTAL leech hazard, NOT a walking AI enemy. This reframes several audit dimensions: (C) DAMAGE-TAKING is intentionally N/A â€” Bloodsucker_AnimUpdateFromStep sets chara->health = 1 (bloodsucker.c:133) and there is NO health-decrement or death path anywhere in the file; the leech cannot be killed (PSX-faithful, it is anchored to geometry). It has no Ai_Init health pool, no recoil/stun/death FSM. This is correct, not a missing-damage-apply bug. (F) FLOOR/COLLISION fall-through is N/A â€” the enemy never moves and has no gravity/Collision_CharaCollisionSetup call; its collision shapes are derived from a bone matrix each frame, so it cannot fall through the floor.

DATA: Three sources verified. (1) BLOODSUCKER_ANIM_INFOS[30] is real, sized to the 30-entry status range (0x00..0x1D), runtime-seeded via BloodsuckerSeed[30] (kind 0/1/2 -> Anim_BlendLinear/PlaybackOnce/PlaybackLoop, kind 3 -> NULL), Init at pc_port/src/bloodsucker_anim_infos.c:62, REGISTERED at main_pc.c:530-531. (2) data_stubs.c:9 is just a redirect comment, no residual u8[256]={0}. (3) No *_rodata.inc exists and none is needed â€” the bloodsucker has NO AI dispatch function-pointer table (control states are a plain switch in Bloodsucker_Update, not `D_800xxxx[state](chara)`), so the dual-location-data rule that mandates an .inc for groaner/floatstinger does not apply here. Per the specific guidance ('Has anim_infos.c, no rodata.inc') this is the expected/correct shape.

REGISTRATION/LIVE: Confirmed LIVE in both hosts. map3_s03.c:15 does #include "../src/maps/characters/bloodsucker.c"; map7_s02 uses the wrapper Chara_Bloodsucker.c. Wired into charaUpdateFuncs at the Chara_Bloodsucker slot in map3_s03_header.c:154 and map7_s02_header.c:185. Not a dead duplicate.

DAMAGE-DEALING (D): sharedFunc_800D0F28_3_s03 (bloodsucker.c:299) calls func_8008A0E4(1, WEAPON_ATTACK(EquippedWeaponId_Unk69=69 'Bloodsucker attack', AttackInputType_Tap), ...). The player reacts via player_control.c:8556 case 69 (bite-grab state, moveSpeed 1.6). The once-per-swing guard is genuinely effective: bloodsucker.c:297 sets field_44.field_0=1, so on entry var_t1=1 and func_8008A0E4 resets the field_44.field_8 bitmask only when the anim status loses its active bit (combat:157) or a brand-new swing begins (combat:162) â€” neither fires while the bite loops, so the player is hit once per bite cycle, not per frame. No high-FPS multiplication and no per-frame unconditional damage.

FPS GATE (G): The status comparisons in Control_2/3/4 use '==' but they compare anim STATUS IDs (e.g. == (stateStep*2)+9), not keyframes or anim.time, so they are state-machine equality, NOT the '== exactKeyframe' high-FPS anti-pattern. All time/timer arithmetic is Q12_MULT_FLOAT_PRECISE(g_DeltaTime,...).

MINOR NON-DEFECT: Bloodsucker_Update has no top-level 'if (g_DeltaTime != Q12(0))' wrapper (unlike groaner). This is harmless and PSX-faithful: timers use g_DeltaTime multiplies (freeze on pause), func_8008A0E4 self-guards on g_DeltaTime==0, and SD_Call SFX are latched behind BloodsuckerFlag_0/_1. The continuous Sfx_WithFalloffAndPitchPlay could re-issue on a paused frame, but this matches the original (the function has no such guard on PSX either) and is not a port regression. Not flagged as a defect.

I did not run the game; this is a static read of the live source, the anim table + Init, the shared combat func (func_8008A0E4 swing/bitmask logic), the npc_main.c dispatch, and a diff against the alpha6 reference (identical).</parameter>
</invoke>

- Live: ['C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/bloodsucker.c', 'C:/Claude/silenthill/silent-hill-decomp/pc_port/src/bloodsucker_anim_infos.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map7_s02/Chara_Bloodsucker.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map3_s03/map3_s03.c']
- No defects found.

### incubus (id 22) — role: boss
- **Status:** FUNCTIONAL with one cosmetic/data defect. The Incubus final boss is correctly ported across the gameplay-critical dimensions: LIVE and registered (map7_s03_header.c:150, via Chara_Incubus.c #include), real anim table (INCUBUS_ANIM_INFOS[12] + Init at main_pc.c:581-582, anim-status index in-bounds), FPS-safe damage-taking (once-per-frame health subtract + single Chara_DamageClear) and FPS-safe projectile damage-dealing (once-per-projectile latch field_4E8), faithful boss model (huge Q12(40000) HP gated by a bossFightTimer_F4 scripted-death countdown rather than pure HP drain; 10x weak-point damage window via CharaFlag_Hit clear/restore), real per-frame GTE-derived hit radius (not a hardcoded fallback), and shared floor collision via Chara_MovementUpdate. x64-clean (switch dispatch, no fn-ptr-table truncation, NULL-guarded boneCoords). Zero SH_PC_PORT band-aids in incubus.c (pure clean decomp). The ONLY defects are two un-extracted SFX ROM tables (D_800EC8C8 / D_800EC8FC) left as zero-stubs, which mute all Incubus sound effects and leave a latent OOB read on D_800EC8FC for late keyframes. No crash, no gameplay-breaking bug.
- Live: ['C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/incubus.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map7_s03/Chara_Incubus.c', 'C:/Claude/silenthill/silent-hill-decomp/pc_port/src/incubus_anim_infos.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map7_s03/map7_s03_2.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map7_s03/map7_s03_header.c']
- **Missing fixes:** ['data_extraction: D_800EC8C8 (s_SfxVolume[13]) and D_800EC8FC (u8[340]) Incubus SFX ROM tables NOT extracted -> still zero-stubs (boss silent + latent OOB read on D_800EC8FC[256..339])']
- **[C] medium/data_zerostub** (conf high) `C:/Claude/silenthill/silent-hill-decomp/pc_port/src/stubs/data_stubs.c:473`
    - D_800EC8C8 (the Incubus SFX-volume table) is a zero-stub `u8 D_800EC8C8[256] = {0}`, but the header declares it as `s_SfxVolume D_800EC8C8[13]` (incubus.h:42). The exe-resident stub is imported by map7_s03.dll (confirmed via nm: __imp_D_800EC8C8), so the runtime table is all zeros. func_800DDB68 (incubus.c:130) reads `D_800EC8C8[soundIdx].id_0` (=0) and `.volume_2.val16` (=0) and calls Sfx_WithFlagsPlay(0, ..., 0, ...) -> SFX id 0 at volume 0 = the Incubus boss plays NO sound effects (roars / attack / hurt cues). Same class as the prior cat-anim / cutscene-voice zero-stub bugs. Not a crash; cosmetic but a real fidelity defect for a final boss. Also a cross-TU type mismatch (u8[256] vs s_SfxVolume[13]).
    - *Fix:* Extract the real 13-entry s_SfxVolume table from disc (PSX 0x800EC8C8 in MAP7_S03.BIN) the same way the anim infos were extracted, and define it as the correct type (e.g. a real `s_SfxVolume D_800EC8C8[13] = {...}` in a pc_port/src/incubus_sfx.c or via the extract_map_data.py EXTRA_SYMBOLS path), replacing the zero-stub. Remove the conflicting u8[256] stub so the declared type matches.
    - *Verify:* Verified data_stubs.c:473 is `u8 D_800EC8C8[256] = {0};` and incubus.h:42 declares `extern s_SfxVolume D_800EC8C8[13];` (a real 4-byte struct per shared.h:98-106). incubus.c:130 (func_800DDB68) reads D_800EC8C8[soundIdx].id_0 and .volume_2.val16 and passes them to Sfx_WithFlagsPlay. incubus.c is LIVE (Chara_Incubus.c includes it). The zero-stub is the only definition of D_800EC8C8 anywhere (grep across pc_port/ + src/); no extraction exists. Therefore the runtime table is all zeros -> SFX id 0 at volume 0 -> incubus plays no SFX. The cross-TU type mismatch (u8[256] vs s_SfxVolume[13]) is also real. All cited facts hold; cosmetic/fidelity defect, not a crash. Confirmed.
- **[C] medium/data_zerostub** (conf high) `C:/Claude/silenthill/silent-hill-decomp/pc_port/src/stubs/data_stubs.c:474`
    - D_800EC8FC (the keyframe-index -> SFX-index lookup, declared `u8 D_800EC8FC[340]` in incubus.h:44) is zero-stubbed as `u8 D_800EC8FC[256] = {0}`. func_800DDBA4 (incubus.c:135) returns `D_800EC8FC[idx]` and is the sole source feeding func_800DEE90's per-keyframe SFX trigger (incubus.c:969-979). All-zero means the keyframe-driven SFX index is always 0, so no per-animation SFX ever fires (and the [340] table is also under-sized at [256], so indices 256-339 read out of the stub's bounds -> garbage/OOB read for late keyframes; anim.time can reach keyframe 338 per the anim seeds). Pairs with the D_800EC8C8 defect: even if D_800EC8C8 were filled, this table being zero/short would still mute keyframe-cued sounds and risk an OOB read.
    - *Fix:* Extract the real 340-byte D_800EC8FC keyframe->SFX map from disc (PSX 0x800EC8FC) and define it with the correct [340] size, replacing the under-sized zero-stub. This both restores keyframe-cued SFX and removes the latent OOB read for keyframes 256-339.
    - *Verify:* Verified data_stubs.c:474 is `u8 D_800EC8FC[256] = {0};` while incubus.h:44 declares `extern u8 D_800EC8FC[340];` -- the stub is both zero-filled AND under-sized ([256] vs [340]). incubus.c:135 (func_800DDBA4) returns D_800EC8FC[idx]; func_800DEE90 (incubus.c:969-979) calls it with FP_FROM(anim.time, Q12_SHIFT) for prevSfxIdx and sfxIdx, gated by `sfxIdx != 13 && sfxIdx != prevSfxIdx` then func_800DDB68. INCUBUS_ANIM_INFOS seeds (pc_port/src/incubus_anim_infos.c) have endKeyframeIdx values reaching 338, so idx can exceed 255 -> OOB read against the [256] stub for keyframes 256-339, and the all-zero contents mute keyframe-cued SFX. Only definition of the symbol is this stub; no extraction exists. Both the muting and the latent OOB read are real. Confirmed.

### unknown23 (Chara_Unknown23, e_CharaId 23) â€” the Incubator final-boss "lightning-cage" attack phase in map7_s03 (id 23) — role: boss
- **Status:** COMBAT-ACTIVE BOSS PHASE, mostly clean. unknown23 is NOT a standalone enemy and is NOT cutscene-only: it is the Incubator final boss's lightning-cage / energy-shield attack phase. func_800E9498 (map7_s03_3.c:3796) hot-swaps the Incubator's charaUpdateFuncs slot to Unknown23_Update during that phase, and func_800E94AC swaps it back to Incubator_Update. It has full boss health (Q12 30000), deals contact damage to Harry (AttackReceived 68, dist < 2.5), and renders a procedural lightning-cage grid effect. The character LOGIC is clean, FPS-safe, x64-safe, with a real registered anim table and correct once-per-tick damage apply + Chara_DamageClear. The ONE real defect is data: D_800ECA50 (the lightning-cage color ROM LUT) is an un-extracted zero-stub with an s32[]-vs-u8[256] size mismatch -> the cage effect draws black AND triggers a per-cell out-of-bounds read every frame the effect is active. Fix = extract the real table from MAP7_S03.BIN (anim_infos/rodata extraction pattern) with the correct s32 element type and >=256 length, and remove the data_stubs zero-stub. Secondary cosmetic: D_800F3DB8 should be a q19_12 scalar, not u8[256], to match the header.
- Live: ['C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/unknown23.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map7_s03/Chara_Unknown23.c', 'C:/Claude/silenthill/silent-hill-decomp/pc_port/src/unkkown_23_anim_infos.c', 'C:/Claude/silenthill/silent-hill-decomp/include/maps/characters/unknown23.h']
- **Missing fixes:** ['data extraction (ROM LUT): D_800ECA50 lightning-cage color table is NOT extracted -- still a u8[256]={0} zero-stub with an s32[] type mismatch (the one real defect). This is the same class of bug as the cat/grey-child zero-stub tables but for a boss visual-effect LUT rather than an anim table.']
- **[C] high/data_zerostub** (conf high) `C:/Claude/silenthill/silent-hill-decomp/pc_port/src/stubs/data_stubs.c:475`
    - D_800ECA50 is a read-only ROM color/intensity LUT for the boss's procedural lightning-cage effect, but it is a zero-stub. It is declared in the header as 'extern s32 D_800ECA50[]' yet defined in data_stubs.c as 'u8 D_800ECA50[256] = {0}'. Two problems: (1) ALL-ZERO data means the lightning-cage effect (func_800DF418 -> func_800DF458 POLY_G4 grid) draws with zero color = black/invisible spark mesh; (2) TYPE/SIZE MISMATCH: the compiler in unknown23.c sees it as s32[], and func_800DF418 indexes it by a u8 pixel intensity (ptr->field_5D[...] range 0..255). As an s32[] an index of 255 reads byte offset 1020, but the storage is only 256 bytes -> out-of-bounds read of up to ~764 bytes past the buffer every grid cell, every frame the cage renders. The pc_port zero_stub_audit.txt:133-134 already classifies it: '[78] D_800ECA50 INDEXED read (2x) and never written -> ROM table' with 'decl: extern s32 D_800ECA50[]'.
    - *Fix:* Extract the real D_800ECA50 ROM table from disc (MAP7_S03.BIN at PSX VRAM 0x800ECA50) the same way unkkown_23_anim_infos.c / *_rodata.inc were extracted (pc_port/tools/extract_*.py), define it with the correct element type s32 and correct length (>=256 entries so a u8 index 0..255 is in-bounds), and remove the u8[256] zero-stub from data_stubs.c. If it points only at exe-resident data it can live in pc_port/src; since it is pure data with no fn-ptrs it can be a plain extracted .c compiled into the map. Until extracted, at minimum fix the size to 'u8 D_800ECA50[256]' matched to an 'extern u8 D_800ECA50[256]' decl to kill the s32[] OOB read, but the effect will stay black until real data is supplied.
    - *Verify:* Verified all cited lines: data_stubs.c:475 = 'u8 D_800ECA50[256] = {0};'; unknown23.h:40 = 'extern s32 D_800ECA50[];'; unknown23.c:96 = 'return D_800ECA50[ptr->field_5D[(arg1 * 41) + arg0]];'; struct field_5D is u8[31*41] so index range is 0..255; zero_stub_audit.txt:133-134 matches exactly. Both sub-claims hold. (a) Type mismatch is real: unknown23.c's TU sees the s32[] extern decl, so D_800ECA50[i] is computed as base + i*4; the actual storage is only 256 bytes (u8[256]); a u8 index of 255 reads 4 bytes at byte offset 1020-1023, ~764+ bytes past the buffer end -> genuine OOB read every grid cell every frame the cage renders (func_800DF418 is called ~4x per inner-loop iteration across a 30x40 grid). (b) All-zero data means the POLY_G4 cage colors are all zero -> black/invisible spark mesh. The proposed fix (extract real ROM table as s32 with >=256 entries, or at minimum match decl+defn type to kill the OOB) is warranted. This is a real high-severity defect.
- **[C] low/data_zerostub** (conf high) `C:/Claude/silenthill/silent-hill-decomp/pc_port/src/stubs/data_stubs.c:582`
    - D_800F3DB8 is declared in the header as a single scalar 'extern q19_12 D_800F3DB8' (the lightning-effect state-machine timer: D_800F3DB8++, D_800F3DB8 += g_DeltaTime, compared to Q12(1.5f), reset to 0/1) but defined in data_stubs.c as 'u8 D_800F3DB8[256] = {0}'. Functionally benign on x64 (the s32 scalar accesses land in the first 4 bytes of the 256-byte buffer and it is runtime-written, not ROM data), but the type mismatch is a latent footgun and inconsistent with the header. Not a behavioral bug.
    - *Fix:* Change the data_stubs definition to match the header: 'q19_12 D_800F3DB8 = 0;' (it is runtime state, zero-init is correct). Cosmetic / correctness-hardening only.
    - *Verify:* Verified: data_stubs.c:582 = 'u8 D_800F3DB8[256] = {0};'; unknown23.h:42 = 'extern q19_12 D_800F3DB8; // Timer.' (scalar); used purely as a scalar at unknown23.c:320 (switch), 328 (++), 342 (+= g_DeltaTime), 343/345 (compare to Q12(1.5f), reset 0), 353 (=0), 372 (=1). The decl/defn type mismatch (scalar in header, 256-byte array in stub) is real. The claim correctly characterizes it as functionally benign on x64 (scalar accesses hit the first 4 bytes of the buffer, and it is runtime-written state not ROM data) and explicitly labels it 'Not a behavioral bug' with a cosmetic/correctness-hardening fix ('q19_12 D_800F3DB8 = 0;'). The defect-as-described is accurate, so confirmed at the stated low severity. No game-behavior impact.
- **[U] low/collision_floor** (conf low) `C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/unknown23.c:888`
    - OT bucket index in the lightning-cage drawer is derived from an unclamped otz: func_800DF458 does 'ot = &ot[ptr->field_584 >> 1]' where field_584 = func_800DF90C() = RotTransPers otz of the boss origin. This is the twinfeeler-class pattern (unbounded otz-derived OT index can overrun g_OrderingTable0). However it is identical to the sibling Incubus code and is PSX-faithful (origin point is on-screen, otz bounded in practice), so this is an observation to verify, not a confirmed introduced defect. Listed under collision_floor only because no 'ot_clamp' dimension exists.
    - *Fix:* If disappearing-geometry / OT corruption is ever observed during the Incubator lightning phase, clamp the bucket index to [0, OT_SIZE-1] as done for twinfeeler (project_twinfeeler_ot_clamp). Do not change preemptively; verify against PSX first.
    - *Verify:* Verified the cited code: unknown23.c:121 = 'ot = &ot[ptr->field_584 >> 1];' and unknown23.c:332 = 'ptr->field_584 = func_800DF90C();', where func_800DF90C returns a RotTransPers otz (confirmed: it ends 'return RotTransPers(...)'). So an unclamped otz-derived OT bucket index does feed g_OrderingTable0[idx].org indexing, matching the twinfeeler-class overrun pattern in principle. However the claim itself states this is PSX-faithful (identical to sibling Incubus code, origin point on-screen so otz bounded in practice) and is 'an observation to verify, not a confirmed introduced defect', with the proposed action being 'Do not change preemptively; verify against PSX first.' Whether the index ever actually exceeds the OT bounds depends on runtime otz values during the lightning phase, which cannot be determined without running the game. Therefore neither a confirmed defect nor cleanly refuted -> uncertain, consistent with the claim's own framing.

### monster_cybil (id 24) — role: boss
- **Status:** CLEAN â€” no defects found across dimensions A-G. monster_cybil is a fully-faithful, complete boss port and one of the cleaner enemies in the codebase. All required PC-port fixes (anim data extraction + Init registration, keyframe-constant extraction into the DLL, two distinct x64 cross-TU/register fixes, once-per-swing damage via shared func_8008A0E4, g_DeltaTime/TIMESTEP_SCALE_30_FPS timing) are PRESENT and correct.</parameter>
<parameter name="notes">LIVE CONFIRMED: monster_cybil.c is compiled via #include in src/maps/map6_s04/Chara_MonsterCybil.c (build object present in build.ninja), registered as MonsterCybil_Update in map6_s04_header.c:171 charaUpdateFuncs[Chara_MonsterCybil=24]. Edit monster_cybil.c (it is live; there is no dead duplicate).

DIMENSION-BY-DIMENSION:
A (data_zerostub): CLEAN. Anim table real (44 entries, seed+Init, registered main_pc.c:567-568). Keyframe constants D_800EA* are real ROM values in map6_s04_extracted_data.c, compiled into the DLL. No residual enemy zero-stub is actually read by the DLL; the data_stubs.c D_800CB118/D_800CB178/D_800EBB48 (u8[256]={0}) are exe-side and shadowed by the DLL's own const tables + extracted_data (groaner-style dual-location pattern). D_800EA* family is NOT present in data_stubs.c at all.
B (x64_pointer): CLEAN. Two documented fixes present (variableFunc DLL-side re-bind at monster_cybil.c:355; func_800D8898 dual-arg signature at :292). No s32/u32 ptr truncation, no struct-size assumption, no raw-addr cast left active, no stack-frame aliasing. The const data tables (D_800CB*) are 2D/1D arrays indexed by bounded field_FE (0..9) / keyframeIdx (<24 guarded) / status â€” no overrun.
C (damage_taking): CLEAN. health 4000; damage halved then applied once + cleared (lines 458/465/466); death event-driven (EventFlag_446); phase shift health<2000. Hit radius from real cylinder data, not hardcoded.
D (damage_dealing): CLEAN. Attacks go through shared func_8008A0E4 (real WEAPON_ATTACK ids) with the once-per-swing sp14&sp10 guard; gated by anim-time/keyframe windows. The grab hold-damage `playerChara.damage.amount = g_DeltaTime * 4` (line 1705) is intentional continuous-grab damage and is correctly g_DeltaTime-scaled (FPS-safe), zeroed on grab-break (line 1715).
E (anim): CLEAN. Real 44-entry table; func_800D99E4 plays lower+upper body via animInfo->playbackFunc; no zero/frozen table; variable-duration callback bound.
F (collision_floor): CLEAN. Collision_WallDetect + per-state vy handling; boss manages own vertical (position.vy forced to floor offsets in knockdown/teleport states); no NULL-IPD fall-through observed (boss arena is a fixed loaded chunk).
G (fps_gate): CLEAN. IMPORTANT ANALYSIS: the many `keyframeIdx == <const>` checks are NOT high-FPS bugs. g_DeltaTime is seconds-per-frame (1/60 at 60fps), and Anim_PlaybackOnce advances keyframeIdx by Q12_MULT_PRECISE(duration, g_DeltaTime). At HIGHER FPS the per-frame keyframe step gets SMALLER, so keyframeIdx passes through MORE integer values â€” exact-equality checks become MORE reliable, never skipped. Additionally end-of-animation transitions are safe because Anim_PlaybackOnce CLAMPS newTime to endKeyframeIdx (bodyprog_anim_800445A4.c:304-308), parking keyframeIdx exactly on the end frame for >=1 frame. Mid-anim exact checks (==38 line 698, ==101 line 1720) land reliably at >=30fps; ==101 missing only slightly alters a procedural angle (else-branch is bounded), and ==38 is at low timestep. No '==exactKeyframe' COMBAT-DAMAGE gate exists (damage uses func_8008A0E4 windows); the keyframe equalities are state-machine transitions, which are FPS-safe here.

ADSR SWISH-LOOP HISTORY (per task guidance): the swish-loop ADSR scope fix (func_800E3F30 scope-on / func_800E4714 restore) lives in src/maps/map7_s03/map7_s03_3.c â€” that is the Cybil CUTSCENE/scene 'Cybil portion', a DIFFERENT system from the map6_s04 possessed-Cybil BOSS audited here. It does NOT touch monster_cybil.c and is not a concern for this boss. monster_cybil plays its own one-shot SFX via Sfx_WithFlagsPlay / sharedFunc_800D9188/908C gated by sharedData_800D16E4_2_s01 (a re-trigger latch), not a looping seq voice â€” no ADSR-scope issue in the boss code.

SUSPECT/UNVERIFIED: none requiring action. I did not runtime-test the fight; this is a static read. The audit is exhaustive across the full 2233-line file plus anim_infos, extracted_data, header registration, and the anim playback engine.
- Live: ['C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/monster_cybil.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map6_s04/Chara_MonsterCybil.c', 'C:/Claude/silenthill/silent-hill-decomp/pc_port/src/monster_cybil_anim_infos.c', 'C:/Claude/silenthill/silent-hill-decomp/pc_port/build_gen/extracted_data/map6_s04_extracted_data.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map6_s04/map6_s04_header.c']
- No defects found.

### incubator (id 36) — role: cutscene
- **Status:** CLEAN (cutscene-only character). The Incubator (charaId 36) in map7_s03 is NOT a fought combat boss -- it is the good+ ending cutscene Incubator, driven entirely by DMS cutscene transform data (Dms_CharacterTransformGet(&npcs[4].position, &npcs[4].rotation, \"MAR\", g_Cutscene_Timer, ...) at map7_s03_3.c:931). Incubator_Update (incubator.c:7) contains ONLY: anim-status dispatch by controlState (func_800D3E18), DMS/heading-driven movement with vertical pinned to Q12(0.0f) each frame (func_800D3C80), and Chara_AnimUpdate. There is NO health, NO damage-taking path (no health-=damage.amount / Chara_DamageClear), NO attack/damage-dealing (no func_8008A0E4), NO AI dispatch table, and NO collision/floor resolution beyond Chara_CollisionReset in Incubator_Init. This matches the original PSX decomp exactly (the commented-out reference table in incubator.h confirms the 8-entry anim infos and the function is faithful clean decomp). All applicable PC-port fixes are present. No defects found across dimensions A-G: data is fully extracted and Init-registered (A clean), no pointer truncation/struct-size/aliasing issues (B clean), no damage-taking logic exists because the character takes no damage (C N/A), no damage-dealing logic exists because it does not attack (D N/A), anim infos are real and non-zero (E clean), no floor/collision combat code so no fall-through risk -- vy is forced to Q12(0.0f) (F N/A/clean), and no == exactKeyframe or raw frame-count gates exist (G clean).
- Live: ['C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/incubator.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map7_s03/Chara_Incubator.c', 'C:/Claude/silenthill/silent-hill-decomp/pc_port/src/incubator_anim_infos.c']
- No defects found.

### bloody_incubator (id 37) — role: boss
- **Status:** CLEAN. bloody_incubator (charaId 37) is the final-boss bloody/transformed form and is a SCRIPTED CUTSCENE/SET-PIECE boss puppet, NOT an autonomous health-pool combat enemy. The entire bloody_incubator.c (131 lines) only: (1) plays the animation matching an externally-set controlState (BloodyIncubatorControl_1..11 -> BloodyIncubatorAnim_*), (2) integrates moveSpeed/fallSpeed into position via the standard OVERFLOW_GUARD + Q12_MULT_PRECISE + g_DeltaTime math (faithful, FPS-safe), and (3) Chara_CollisionReset on init. It has NO health init, NO damage-taking (no health -= damage.amount / Chara_DamageClear), NO damage-dealing (no func_8008A0E4), NO AI dispatch table, NO collision sizing, NO floor clamp of its own. That is CORRECT for this enemy: the Incubator->BloodyIncubator final boss is orchestrated entirely by map7_s03_3.c, which drives controlState via Event_CharaAnimCmdExecute(CharaAnimCmd_SetState, &g_SysWork.npcs[5], N) (states 1-11 at map7_s03_3.c:2143/2466/2494/2508/2527/2555/2580/2602/3149/3209) and loads/buffers it via func_800E9260(Chara_BloodyIncubator, ...) gated by cutscene timers and event flags. Phase progression (Incubator transforming into BloodyIncubator) is the scripted scene at case 23/26 (func_800E9444(Chara_BloodyIncubator, &g_SysWork.npcs[5])), and 'death'/ending is event-flag/timer driven, not an HP bar. All audit dimensions A-G are clean: (A) real non-zero anim table + Init registered, no zero-stub; (B) no x64 issues; (C/D) no damage logic exists by design (scripted boss); (E) anim table correct (24 entries=12 anims x blend+playback, status 0x00-0x17, matches header enum 0-11); (F) position.vy hard-set to Q12(0.0f) each frame (no fall-through possible); (G) no exact-keyframe/frame-count gates. The bone-count overflow concern from the task is already FIXED and present. No defects found.
- Live: ['C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/bloody_incubator.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map7_s03/Chara_BloodyIncubator.c', 'C:/Claude/silenthill/silent-hill-decomp/pc_port/src/bloody_incubator_anim_infos.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map7_s03/map7_s03_3.c']
- No defects found.

### flauros (id 40) — role: cutscene
- **Status:** CLEAN - non-combat cutscene artifact, no defects. Flauros (the Flauros artifact/talisman) is NOT a combat enemy. It is registered at map6_s04_header.c:187 charaUpdateFuncs[40]=Flauros_Update and is LIVE (#include of flauros.c at map6_s04_2.c:37). It is spawned only inside the final sealing cutscene sequence (map6_s04_2.c:2817 Chara_Load / 2906 Chara_Spawn, driven by Event_CharaAnimCmdExecute / Event_DisplayMapMsgWithAudio / Event_CutsceneTimerAdvance / Sfx). Flauros_Update (flauros.c:5-45) does only cosmetic work: init control state + idle anim on first call, compute a smooth light-ray grow-in scale from anim.time, Math_MatrixTransform the pose into boneCoords, play the anim via FLAUROS_ANIM_INFOS[status].playbackFunc, and scale bones 6-10 (light rays). It has NO health init, NO damage-taking, NO damage-dealing, NO collision/cylinder setup, and NO floor handling - so combat dimensions C/D/F and fps-gate dimension G are not applicable. Data (A) and pointer (B) and anim (E) dimensions are all clean. No KNOWN PC-PORT FIXES are missing for this character.
- Live: ['C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/flauros.c', 'C:/Claude/silenthill/silent-hill-decomp/pc_port/src/flauros_anim_infos.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map6_s04/map6_s04_2.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map6_s04/map6_s04_header.c']
- No defects found.

### little_incubus (id None) — role: cutscene
- **Status:** Healthy / not a combat enemy. little_incubus is a CUTSCENE-ONLY actor in the bad ending (map7_s03): it is loaded into the cutscene NPC buffer (map7_s03_3.c case 45-48, func_800E9260/func_800E9444 with Chara_LittleIncubus) and its update (little_incubus.c, LittleIncubus_Update @0x800D5BC8, 38 lines) only runs a procedural sin-based grow/appear scaling of bones 1 and 7 over time. It has NO health, NO damage taking, NO damage dealing, NO AI dispatch table, NO attack/weapon code, and NO collision/floor/movement logic, so dimensions C (damage_taking), D (damage_dealing) and F (collision_floor) are N/A. It is correctly LIVE (Chara_LittleIncubus.c wrapper) and correctly registered at charaUpdateFuncs index 41. The anim table is real and properly initialized; FPS gating is correct (g_DeltaTime-scaled). The single defect found is a low-severity type mismatch on the D_800EDA00 grow-accumulator (defined as u8[256] in data_stubs.c but declared/used as s32) â€” it functions correctly in practice but should be a plain `s32 D_800EDA00 = 0;` to match the header and the true 4-byte PSX symbol. No x64-pointer, anim-stuck, OT-clamp, floor-clamp, or high-FPS combat bugs apply to this actor.
- Live: ['C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/little_incubus.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map7_s03/Chara_LittleIncubus.c', 'C:/Claude/silenthill/silent-hill-decomp/pc_port/src/little_incubus_anim_infos.c', 'C:/Claude/silenthill/silent-hill-decomp/include/maps/characters/little_incubus.h', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map7_s03/map7_s03_header.c']
- **[C] low/data_zerostub** (conf high) `C:/Claude/silenthill/silent-hill-decomp/pc_port/src/stubs/data_stubs.c:516`
    - Type mismatch on D_800EDA00. The header declares it as a scalar (include/maps/characters/little_incubus.h:27 `extern s32 D_800EDA00;`) and little_incubus.c uses it as a scalar accumulator (line 18 `D_800EDA00 = 0;`, line 21 `D_800EDA00 += Q12_MULT_FLOAT_PRECISE(g_DeltaTime, 10.0f);`, line 22 `clampedScale = FP_TO(D_800EDA00, Q12_SHIFT) / Q12(80.0f);`). But data_stubs.c:516 defines it as `u8 D_800EDA00[256] = {0};` â€” the generic auto-generated 256-byte zero-stub, not a real 4-byte s32. The original PSX symbol is a 4-byte s32 (the consecutive symbols D_800EDA00 / D_800EDA04 / D_800EDA08 / D_800EDA0C in data_stubs.c are spaced exactly 4 bytes apart, proving each is a single 32-bit word, not a 256-byte array). This is a cross-TU type conflict (technically UB). In practice with MinGW the linker resolves to the array base and the s32 accesses hit the first 4 bytes, so the grow effect still works and the cutscene is NOT visibly broken; severity is low. It should still be corrected to match the header and PSX reality (and it wastes 252 bytes / masks the true symbol type).
    - *Fix:* In pc_port/src/stubs/data_stubs.c:516 replace `u8 D_800EDA00[256] = {0};` with `s32 D_800EDA00 = 0;` to match the `extern s32` declaration in little_incubus.h and the actual 4-byte scalar usage (matching the 4-byte spacing of the adjacent D_800EDA04/08/0C symbols).
    - *Verify:* All cited facts re-verified. data_stubs.c:516 is the sole definition and reads `u8 D_800EDA00[256] = {0};` (an array). include/maps/characters/little_incubus.h:27 declares `extern s32 D_800EDA00;` (a scalar). little_incubus.c uses it strictly as a scalar accumulator: line 18 `D_800EDA00 = 0;`, line 21 `D_800EDA00 += Q12_MULT_FLOAT_PRECISE(g_DeltaTime, 10.0f);`, line 22 `clampedScale = FP_TO(D_800EDA00, Q12_SHIFT) / Q12(80.0f);`. The file is LIVE, compiled via src/maps/map7_s03/Chara_LittleIncubus.c (#include "characters/little_incubus.c") into map7_s03.dll. The adjacent D_800EDA04/08/0C are confirmed separate symbols (used independently in map7_s03_3.c, declared s32/u8 scalars in map7/map7_s03.h), so the 4-byte symbol spacing does prove each is a single word, not one big array. This is a genuine cross-TU type conflict (extern s32 vs definition u8[256]) and is technically UB. The claim is honest about severity: in practice MinGW resolves to the array base (build disasm shows __fu16/__fu17_D_800EDA00 auto-import thunks) and the s32 accesses hit the first 4 bytes, so the grow effect works and the cutscene is not visibly broken. It is therefore a real low-severity type-hygiene/correctness defect (and wastes 252 bytes / masks the true symbol type). The proposed fix (change to `s32 D_800EDA00 = 0;`) matches the header and PSX reality and is warranted.

### ghost_doctor (id 42) — role: cutscene
- **Status:** CLEAN (cutscene-only NPC). Ghost Doctor is a non-combat, scripted/posed figure shown in the map7_s02 ending cutscene (spawned alongside Dahlia + Kaufmann inside Player_ControlFreeze/CutsceneBorder_ForceShow at map7_s02_2.c:1716-1725). Its entire LIVE update (ghost_doctor.c, 52 lines) does only: Chara_CollisionReset on init, set idle anim GhostDoctorAnim_1 once via a stateStep guard, build the root rotation matrix (Math_RotMatrixZxyNegGte), copy position into the root bone, and play the anim via the shared Chara_AnimUpdate(GHOST_DOCTOR_ANIM_INFOS). No health, no damage taking/dealing, no AI dispatch table, no movement, no floor/IPD collision, no GTE OT emission of its own. The data layer is fully correct and faithfully matches the commented reference table in ghost_doctor.h:26-31. No defects found across any audit dimension.
- Live: ['C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/ghost_doctor.c', 'C:/Claude/silenthill/silent-hill-decomp/src/maps/map7_s02/Chara_GhostDoctor.c', 'C:/Claude/silenthill/silent-hill-decomp/pc_port/src/ghost_doctor_anim_infos.c', 'C:/Claude/silenthill/silent-hill-decomp/include/maps/characters/ghost_doctor.h']
- No defects found.

### parasite (id 43) — role: cutscene
- **Status:** CLEAN - cutscene-only character, no defects found
- Live: ['C:/Claude/silenthill/silent-hill-decomp/src/maps/characters/parasite.c', 'C:/Claude/silenthill/silent-hill-decomp/pc_port/src/parasite_anim_infos.c']
- No defects found.

## Prioritized fix plan
All 16 confirmed defects were spot-checked against the live source and hold. They cluster into a few PATTERNS, and only two patterns actually touch the user-reported gameplay problems; the rest are data-hygiene/cosmetic. Priority order driven by user reports:

USER-REPORTED, GAMEPLAY-IMPACTING (do first):
1. Air Screamer "barely attacks / just shoves" = TWO compounding defects: (a) the universal PC bite trigger only recognizes HoverBiteAttack, so StandBiteAttack and (the wild-map common) GlideScratchAttack deal ZERO damage on the 5 non-cafe maps, and (b) the NULL-ptr collision fallback leaves cylinder.field_2 (chara-to-chara push radius) hardcoded at Q12(1.5f) â€” a ~2.5x oversized shove bubble. Fix both: widen isBiteActive to the THREE cone anims (NOT four â€” HoverInjuredBiteAttack(9) is a speed-switch case, never calls the cone applier; the defect JSON's own verify note flags this), and replace field_2=1.5 with the real per-keyframe extent. These two changes directly resolve the top user complaint.

USER-REPORTED but NEEDS DATA (cannot edit safely without user input):
- split_head "takes too much damage to kill" and nurses "maybe die too fast" and "bosses do too much damage (recently reduced - verify)": the audit found NO confirmed health/damage VALUE defect for any of these. The two split_head defects that ARE confirmed are cosmetic FPS gates (blood spurt, footstep SFX), not damage-taking. So the user's split_head/nurse/boss complaints have no verified code root in this defect set â€” they require the user to supply target PSX behavior / health numbers before any value edit. Do NOT change health constants on a hunch (memory rule: never re-apply reverted/value fixes without confirmation).
- "sewer monsters fall through floor": NOT present as a confirmed defect in this set. The known fall-through root (Collision_CharaCollisionSetup NULL-IPD default groundHeight=Q12(8.0), collision.c:489) is map-chunk-loading, not per-enemy. Needs a repro/map name from the user before action.

DATA ZERO-STUBS (medium fidelity, safe, mechanical): incubus SFX tables (D_800EC8C8, D_800EC8FC) and unknown23 lightning-cage color LUT (D_800ECA50) are real ROM tables still zero-stubbed â€” boss plays no SFX, lightning cage is invisible, AND D_800ECA50/D_800EC8FC carry latent OOB reads (s32[] indexed 0..255 over a 256-byte u8 buffer; [340] table sized [256]). These need disc extraction.

TYPE-MISMATCH / COSMETIC (no behavior change, batch last): several u8[256]={0} stubs vs scalar/typed externs (stalker tuning, hanged_scratcher scratch, D_800F3DB8, D_800EDA00) and dead diagnostic blocks (larval_stalker, air_screamer scaffolding). Pure hygiene.

Recommended sequence: P0 air_screamer damage+shove (1 build/test) -> P1 zero-stub extraction for incubus/unknown23 (extract + build/test) -> P1 air_screamer path consolidation (after #1 verified in a real fight) -> P2 type-mismatch cleanup batch -> P3 cosmetic FPS gates + dead code. Health/damage-value questions are gated on user confirmation.

### [P0] Air Screamer damage-dealing: recognize all cone-attack anims + shrink shove radius
- Enemies: ['air_screamer', 'night_flutter']
- Change: TWO edits in src/maps/characters/air_screamer.c. (1) Line 97: widen isBiteActive from only ANIM_STATUS(AirScreamerAnim_HoverBiteAttack,true) to also include AirScreamerAnim_StandBiteAttack and AirScreamerAnim_GlideScratchAttack so the universal top-level PC bite trigger (lines 155-178) fires for the wild-map attacks too. CRITICAL: do NOT add HoverInjuredBiteAttack(9) â€” the defect JSON's verify note confirms anim 9 is a speed-computation switch case (line 13393) that never calls the cone applier func_8008A3E0; adding it would fabricate a non-vanilla attack. Keep the s_biteDamaged per-swing lockout and g_AsBiteCooldown gate so each attack lands once. (2) Line 13737: replace airScreamer->collision.cylinder.field_2 = Q12(1.5f) with the real data-driven extent sharedData_800CAA98_0_s01.field_D70[sp10][1] (the same ~0.6 source the radius now uses on line 13736) so the chara-to-chara push bubble matches PSX instead of shoving Harry from 1.5u away.
- Risk: Low-to-medium. Edit (1) is additive (more anims trigger the existing, already-shipped damage path) so it cannot reduce existing behavior, only restore missing damage; the once-per-swing lockout prevents multi-hit. Edit (2) shrinks the push radius â€” small risk the AS becomes slightly easier to walk into, but matches PSX/groaner reference. Both are behind existing #ifdef SH_PC_PORT blocks. The distSqr<Q12(16.0f) reach in the universal block is generous; verify it still feels right for GlideScratchAttack (a fast swoop) â€” may want the cone/keyframe window honored, but ship the simple widening first. BUILD/TEST CHECKPOINT: build via msys2 bash, fight an Air Screamer on a wild map (not cafe), confirm [AS] BITE HIT traces fire for GlideScratch and Harry takes HP damage not just shove.

### [P1] Extract real ROM data tables to replace enemy zero-stubs (boss SFX + lightning-cage LUT)
- Enemies: ['incubus', 'unknown23']
- Change: Extract three ROM tables from disc and replace the u8[256]={0} stubs in pc_port/src/stubs/data_stubs.c, matching declared types. (1) D_800EC8C8 (incubus.h:42 s_SfxVolume[13]) from MAP7_S03.BIN @0x800EC8C8 â€” incubus boss SFX volume table (currently SFX id 0 vol 0 = boss is silent). (2) D_800EC8FC (incubus.h:44 u8[340]) keyframe->SFX index map â€” also fix the under-size ([256] vs [340]) which causes OOB reads for keyframes 256-339. (3) D_800ECA50 (unknown23.h:40 s32[]) lightning-cage color LUT from MAP7_S03.BIN @0x800ECA50 â€” currently all-zero = invisible black spark mesh AND a real per-frame OOB read (s32[] indexed by u8 0..255 reads byte offset up to 1020 over a 256-byte buffer, every grid cell every frame). Use the existing extract path (pc_port/tools/extract_*.py / extract_map_data.py EXTRA_SYMBOLS) like incubus_anim_infos.c / unkkown_23_anim_infos.c were done. Define each with correct element type and length (>=256 entries for D_800ECA50 so u8 index is in-bounds) and remove the conflicting u8[256] stubs. If real data can't be extracted immediately for D_800ECA50, at minimum change BOTH decl and defn to matching u8[256] to kill the OOB read (effect stays black until data supplied).
- Risk: Low if extracted correctly. Extraction must use the verified PSX VRAM addresses and correct struct layout (s_SfxVolume = id_0 + volume_2.val16). Wrong extraction = wrong SFX ids / cage colors but no crash. The OOB-read fix for D_800ECA50/D_800EC8FC is strictly safer than current state. Must regen build_gen after clone (it's gitignored). BUILD/TEST CHECKPOINT: build with -DSH_BUILD_MAP_DLLS=ON, reach map7_s03 Incubus/Incubator fight, confirm boss SFX play and lightning cage renders with color (not black); check SilentHill.log for no AV.

### [P1] Consolidate Air Screamer to a single canonical damage path
- Enemies: ['air_screamer', 'night_flutter']
- Change: AFTER the P0 fix is confirmed working in a real fight: collapse the three competing PC damage mechanisms to one. Currently coexisting: (a) top-level universal block (air_screamer.c:155-178, '=' write), (b) Control_46 swoop proximity block (8794-8812, '=' write, map0_s01), (c) func_8008A0E4 call (8821, '+=' write, map0_s01). They have independent lockouts (s_biteDamaged vs _asDamagedThisSwoop2 vs HP-delta) that don't cross-suppress, so two can fire same-frame. Preferred end state matching floatstinger: route ALL attacks through func_8008A0E4 (or restore the vanilla func_8008A3E0 cone path by populating field_44.field_18/field_C/field_E from airScreamer->position + rotation.vy when ptr_D48 is NULL â€” like Control_46's _bitePos at 8817), so the shared one-hit-per-swing bitmask guard (bodyprog_combat_8008A058.c:1522-1559) governs damage. Verify EquippedWeaponId_Unk69 damage row D_800AD4C8[69].field_4 is non-zero (it is 6 per item_screens_3.c:147). Then delete the two direct pl->damage.amount=Q12(15.0f) writes (164, 8806) and their ad-hoc lockouts.
- Risk: Medium. The map0_s01 Control_46 paths are currently the ONLY working damage on that map â€” removing them before the canonical path is proven on map0_s01 would regress the cafe fight. Must verify the consolidated path lands on ALL six AS maps before deleting fallbacks. This is why it is sequenced AFTER P0 verification, not bundled with it. BUILD/TEST CHECKPOINT: after consolidation, re-fight AS on BOTH map0_s01 (cafe) and a wild map; confirm single-hit-per-swing damage and no double-hit on either.

### [P1] Health / damage VALUE complaints â€” gather PSX targets before any edit
- Enemies: ['split_head', 'puppet_nurse', 'puppet_doctor', 'floatstinger', 'incubus', 'unknown23']
- Change: NO CODE EDIT until user confirms target values. The audit found the nurse health (400/650/800) and doctor (750) are PSX-faithful and the once-per-swing guard is in place, and floatstinger health/intake-cap are correct â€” so 'die too fast' / 'bosses too strong' have NO confirmed code root in this defect set. split_head's two confirmed defects are cosmetic FPS gates, not damage-taking; there is NO confirmed split_head health-value defect. Per the memory rule 'never re-apply reverted/value fixes without confirmation', do not change any health constant or damage multiplier on a hunch. Instead: present the current extracted values to the user and ask which specific enemy + observed-vs-expected hit count is wrong, then locate the per-enemy apply block (e.g. groaner.c:183 pattern) and the difficulty scaling.
- Risk: High if edited blindly â€” changing health/damage values without a PSX reference is exactly the reverted-fix anti-pattern. Zero risk if we only gather info. NO BUILD until values confirmed.
- **NEEDS CONFIRMATION:** For split_head 'takes too much damage to kill': what weapon and how many hits does it currently take vs how many it should take on the original PSX (and on which difficulty)? Same question for any nurse/doctor that 'dies too fast' and any boss that 'does too much damage'. Also: was the recent boss-damage reduction you mention a change you want kept, or the thing to verify/revert? I will not touch health/damage constants without these targets because guessing them is the reverted-fix trap.

### [P2] Sewer monsters fall through floor â€” need repro before touching collision
- Enemies: ['creeper', 'larval_stalker']
- Change: NO EDIT yet â€” not present as a confirmed defect in this set. The known fall-through-and-reappear root is Collision_CharaCollisionSetup's NULL-IPD fallback (collision.c:489, groundHeight=Q12(8.0)) which fires when the enemy is outside loaded collision chunks â€” a map streaming/chunk-coverage issue, not a per-enemy data bug. Need the user to name the map (sewer = map4_s06 / map5?) and which enemy, then verify Ipd_CollisionDataGet returns non-NULL at the enemy's spawn/move positions and whether the chunk under the enemy is loaded.
- Risk: Editing collision floor logic blind is high risk (affects every chara). Info-gathering only is zero risk. NO BUILD until repro identified.
- **NEEDS CONFIRMATION:** Which map are the 'sewer monsters' in (map name or area), and which enemy type falls through? Does it happen at spawn, while walking a specific spot, or after a room transition? A short clip or the spot's approximate location would let me check whether the IPD collision chunk is loaded there.

### [P2] Fix cross-TU type mismatches on scratch/runtime-state stubs
- Enemies: ['stalker', 'hanged_scratcher', 'unknown23', 'little_incubus']
- Change: In pc_port/src/stubs/data_stubs.c, change the u8[256]={0} stubs to match their header types (all are runtime-written scratch state, zero-init is correct, so NO behavior change): (1) stalker tuning/ring symbols `sharedData_800E39EC[5](s32)/800E3A0C[5](u16)/800E3A18/3A1C/3A20(q19_12)/3A24/3A28/3A2C` per shared.h:233-245; (2) hanged_scratcher sharedData_800DE28C_5_s00 -> q19_12 = Q12(0.0f) (header hanged_scratcher.h:131); (3) D_800F3DB8 -> q19_12 = 0 (unknown23.h:42 Timer); (4) D_800EDA00 -> s32 = 0 (little_incubus.h:27). Each is verified benign on x64 today (over-allocates, accesses land in first 4 bytes) so this is pure correctness-hardening to stop the u8[256] form from masking a real broken stub on future audits.
- Risk: Very low. No behavior change (values are runtime-written before read). Only risk is a typo in element type/count; cross-check each against its header and usage. Confirmed no duplicate-symbol conflicts (map5/map6 extracted_data.c not in CMake build). BUILD/TEST CHECKPOINT: full build (exe + DLLs) must link clean with 0 undefined/duplicate symbols; no runtime test needed (no behavior change).

### [P3] Convert cosmetic exact-keyframe FPS gates to ranges + remove dead diagnostic code
- Enemies: ['split_head', 'larval_stalker', 'air_screamer', 'night_flutter']
- Change: Low-priority cleanups, none affect gameplay damage. (1) split_head.c:386 blood-spurt gate FP_FROM(anim.time,Q12_SHIFT)==35 -> ANIM_TIME_RANGE_CHECK(anim.time,35,36) + once-per-attack latch (mirror the SplitHeadFlag_9 pattern at line 1714) so the cosmetic spurt isn't skipped/repeated at high FPS. (2) split_head.c:95 footstep SFX disjunct keyframeIdx==161 -> (keyframeIdx>=160 && keyframeIdx<162), matching the adjacent block at lines 112-115 that already uses that range. (3) larval_stalker.c:874-884 delete the dead no-op s_prevAmt diagnostic block (reads damage.amount, does nothing). (4) air_screamer.c diagnostic scaffolding (static _swoopLogN/_asPrevSwoopStep_v2/etc and [AS] SH_DBG traces at 135/144/169/175, superseded-attempt comment blocks) â€” remove ONLY after the P0/P1 air_screamer fixes are confirmed in a real fight, since the traces help verify the fix landed.
- Risk: Minimal. Range conversions are PSX-faithful for cosmetic effects. Dead-code removal is safe. Air_screamer scaffolding removal is sequenced last (after fixes verified) so we don't lose diagnostics mid-fix. BUILD/TEST CHECKPOINT: build clean; spot-check split_head blood spurt + footsteps still play at high FPS.

## Open questions
- Air Screamer GlideScratchAttack reach: the universal block uses a flat distSqr<Q12(16.0f) proximity check, but the vanilla path used a per-anim cone (func_8008A3E0). Should the swoop-scratch honor its keyframe 'fangs/claws out' window and heading cone, or is flat proximity acceptable for the first cut? (Recommend ship flat first, refine if it feels off.)
- Does EquippedWeaponId_Unk69 vs the per-anim attack ids used by the vanilla cone path (StandBite/HoverBite/GlideScratch may map to different weapon-attack rows) matter for the damage amount each attack should deal? The universal block hardcodes Unk69 + Q12(15.0f) for all three; PSX may have differentiated. Need confirmation if attack-specific damage is desired.
- For the P1 zero-stub extractions: confirm the disc image path and that pc_port/tools/extract scripts can read MAP7_S03.BIN for the s_SfxVolume[13] / u8[340] / s32[] tables at the cited PSX VRAM addresses. If extraction tooling can't resolve these, the fallback is type/size-matching the stubs to kill the OOB reads while leaving SFX silent / cage black.
- The user said 'bosses do too much damage (recently reduced - verify)' â€” was that reduction a commit we should keep and verify, or a change under suspicion? This affects whether floatstinger/incubus intake caps and player-damage-taken scaling should be left alone or revisited (gated on the P1 confirmation question).